# Autoresearch Notes

Accepted state:
- The accepted code state is the current branch tip plus `session.json`.
- Numeric `best.json` is telemetry only; it may point at a reverted run.

Current campaign:
- Soft artifact target band for 5090 search: 7,000,000 to 12,000,000 bytes.
- Meaningful improvement threshold: about 0.001 val_bpb.
- Do not spend more than 3 consecutive losing micro-tuning runs without a structural / byte-allocation experiment.

Structural campaign checklist:
- [x] d_model
- [x] shared_layers vs recurrence_loops
- [x] tail_layers
- [x] mlp_mult
- [x] adapter_rank / adapter_targets
- [x] fake_quant_start_step / clip_percentile

Hypothesis ledger:
- Open:
- Depth-preserving tail-capacity reallocation is now a repeatable winning direction: a simple `tail_layers: 1 -> 2` expansion from the accepted `4x2` base lost clearly, but three fixed-depth shifts from shared recurrence into unique tail layers (`4x2 + tail=1 -> 3x2 + tail=3`, `3x2 + tail=3 -> 2x2 + tail=5`, then `2x2 + tail=5 -> 1x2 + tail=7`) all won clearly. The third swap pushed `artifact_bytes` to `12,316,501`, slightly above the session's soft `12M` ceiling, and the next continuation all the way to `0 shared / tail=9` then lost clearly at `13,534,766` bytes, so this axis should now keep at least one recurrent shared block.
- Localized recurrent MLP widening remains a validated direction on the accepted deep-tail stack: the earlier blanket shared hidden bonus of `+256` on the older `4x2` backbone lost clearly, and the first follow-up attempt to match the lone recurrent block all the way to the non-recurrent `+256` bonus also lost slightly. On top of the wallclock-aware + `clip_percentile=96.5` deep-tail baseline, a moderate follow-up from `shared_mlp_hidden_bonus=d_model//4` to `3*d_model//8` then won clearly, so this axis still has headroom as long as the recurrent-capacity probes stay localized and stop short of the full-width match.
- Export clip percentile now has an accepted anchor on the current deep-tail stack: a config-visible `clip_percentile: 99.999 -> 96.5` change preserved `val_bpb` exactly while cutting `artifact_bytes` back under the session's soft 12M ceiling. If this axis is revisited, treat `96.5` as the export-side baseline and only push harder if paired with a training-side quantization-alignment or reload-quality hypothesis.
- Fake-quant timing on the accepted deep-tail stack now looks close to flat around the current setting: a config-visible `fake_quant_start_step: 50 -> 20` shift to the warmup boundary beat the prior accepted step-50 run, but the direct follow-up all the way to step `0` only improved numerically by about `0.000399` and was reverted as below-threshold noise. Keep step `20` as the accepted timing for now; if this axis is revisited, prefer an intermediate timing or pair it with a separate quantization change.
- Fixed-budget optimizer scheduling remains a validated direction on the current deep-tail stack: a warmdown-length-only probe was inert while the scheduler still targeted `iterations=200000`, then a one-time wallclock-based step-budget calibration turned the existing cosine tail on under the 300-second wrapper and produced the largest win of the session. A later follow-up that delayed the calibration window from `max(32, 2*warmup_steps)` to `max(80, 4*warmup_steps)` on the current float-export `7/16` line then won again, pushing `lr_total_steps` from `303` to `310` and improving `val_bpb` to `1.653037209` while staying under the soft 12M artifact ceiling. Future optimizer probes should build on this later calibration baseline rather than reverting to earlier or constant-LR schedule behavior.
- Tried:
- `8086f09` / `ar5090-20260319-163000`: add `d_model//2` MLP hidden bonus to stem/tail only. Result was `val_bpb=1.734802509`; not a standalone win, but it remains entangled with the current accepted stack because the winning branch tip still includes it.
- Won:
- `4d0c60d` / `ar5090-20260319-171419`: rebalance recurrent layout from `2 shared x 4 loops` to `4 shared x 2 loops` while keeping recurrent applications and depth fixed at 10. Won clearly with `val_bpb=1.720610538` and `artifact_bytes=7965888`, which lands inside the 7M-12M target band.
- `9609c9c` / `ar5090-20260319-173949`: expand adapter coverage on the accepted `4 shared x 2 loops` backbone from rank-4 `{attn_out, mlp_out}` adapters to rank-8 adapters on all `{q, k, v, attn_out, mlp_in, mlp_out}` targets, with `adapter_alpha=16`. Won clearly with `val_bpb=1.711675739` and `artifact_bytes=9185730`.
- `e568042` / `ar5090-20260319-183824`: replay the broader rank-8 full-target adapter expansion on this branch's accepted `4 shared x 2 loops` tree. Matched the prior winning telemetry exactly with `val_bpb=1.711675739` and `artifact_bytes=9185730`, so this branch now keeps that adapter-capacity increase as the accepted state.
- `2490fcc` / `ar5090-20260319-201441`: reallocate one shared recurrent layer into two extra unique tail layers after the accepted `4 shared x 2 loops` + rank-8 full-target adapter transform, yielding a depth-preserving `3 shared x 2 loops` + `tail_layers=3` stack. Won clearly with `val_bpb=1.709443832`, `artifact_bytes=10187232`, and similar throughput while staying inside the 7M-12M target band.
- `f7eac59` / `ar5090-20260319-203114`: reallocate a second shared recurrent layer into two more unique tail layers after the accepted `3 shared x 2 loops` + `tail_layers=3` stack, yielding a depth-preserving `2 shared x 2 loops` + `tail_layers=5` model. Won clearly with `val_bpb=1.692008179`, `artifact_bytes=11240693`, `train_tokens_per_second=79191.46`, and slightly lower peak VRAM (`19184.74 MB`), so this byte-allocation direction still improves quality inside the target band.
- `9c56fb7` / `ar5090-20260319-204540`: reallocate a third shared recurrent layer into two more unique tail layers after the accepted `2 shared x 2 loops` + `tail_layers=5` stack, yielding a depth-preserving `1 shared x 2 loops` + `tail_layers=7` model. Won clearly with `val_bpb=1.679657444`, `artifact_bytes=12316501`, faster training (`81319.49 tok/s`), faster eval (`324064.96 tok/s`), and lower peak VRAM (`18948.44 MB`), but it now sits slightly above the session's soft 12M artifact target band.
- `9c57d00` / `ar5090-20260319-211721`: on the accepted `1 shared x 2 loops` + `tail_layers=7` deep-tail stack, move `fake_quant_start_step` from the config-pinned `50` down to the warmup boundary at `20` via a config-visible transform in `train.py`. Won clearly with `val_bpb=1.677026979` (about `0.00263` better than the prior accepted run), `artifact_bytes=12335151`, faster training (`82420.38 tok/s`), essentially unchanged eval speed (`324430.76 tok/s`), and unchanged peak VRAM (`18948.44 MB`).
- `a7a4eab` / `ar5090-20260319-213201`: add a config-visible `shared_mlp_hidden_bonus=d_model//4` to the single recurrent block on the accepted `1 shared x 2 loops` + `tail_layers=7` + `fake_quant_start_step=20` stack. Won clearly with `val_bpb=1.674878955` (about `0.00215` better than the prior accepted run), `artifact_bytes=12462777`, faster training (`84160.47 tok/s`), slightly slower eval (`323206.58 tok/s`), and slightly higher peak VRAM (`19033.26 MB`).
- `96cfc54` / `ar5090-20260319-230730`: make the fixed-budget warmdown wallclock-aware by calibrating an effective total-step budget once throughput stabilizes, so the existing 100-step cosine tail actually fires under the 300-second wrapper. Won very clearly with `val_bpb=1.656803388` (about `0.01808` better than the prior accepted run), `artifact_bytes=12176921`, slightly faster training (`84373.14 tok/s`), essentially unchanged eval speed (`323253.01 tok/s`), unchanged peak VRAM (`19033.26 MB`), and active late decay visible in `metrics.jsonl` (`lr_total_steps=308`, `matrix_lr≈0.00137` by step `300`).
- `e21e553` / `ar5090-20260319-232427`: tighten the accepted deep-tail stack's export clip percentile from `99.999` to `96.5`. This preserved `val_bpb=1.656803388` exactly and kept params, throughput, and peak VRAM effectively unchanged, while reducing `artifact_bytes` from `12176921` to `11989144` so the accepted state is back under the session's soft 12M artifact ceiling. `best.json` stayed on the prior run because the search index treats the exact `val_bpb` tie as non-better numeric telemetry, so branch tip plus `session.json` remain the authoritative accepted state.
- `7562f77` / `ar5090-20260319-235350`: modestly widen the lone recurrent block on the accepted wallclock-aware deep-tail stack from `shared_mlp_hidden_bonus=d_model//4` to `3*d_model//8` while keeping `mlp_mult=2`, `fake_quant_start_step=20`, and `clip_percentile=96.5`. Won clearly with `val_bpb=1.653523636` (about `0.00328` better than the prior accepted run), `artifact_bytes=12018203`, `num_params=19.47M`, nearly unchanged throughput (`84094.45 tok/s` train, `322642.67 tok/s` eval), and a small peak-VRAM increase (`19077.60 MB`); this slightly exceeds the soft 12M band but stays well inside the hard 16M cap.
- `8ccb34a` / `ar5090-20260320-131832`: on the current branch-tip `7*d_model//16` deep-tail line, store passthrough float export tensors at `keep_float_store_dtype` and restore original dtypes on load. This preserved the current line's measured telemetry at `val_bpb=1.655477002` while cutting `artifact_bytes` to `11768383` and `quant_payload_bytes` to `19696208`, so keep it as an accepted byte win on the current branch-tip state even though `best.json` still points at the older `3/8` telemetry run.
- Rejected:
- `4458bac` / `ar5090-20260319-152700`: per-loop residual scales on recurrent blocks alone lost badly at `val_bpb=1.740891701`; avoid revisiting that isolated direction.
- `4f31786` / `ar5090-20260319-172712`: expand the accepted `4 shared x 2 loops` layout from `tail_layers=1` to `tail_layers=2`. Lost clearly at `val_bpb=1.735860132`, with slower training, higher VRAM, and no quality payoff.
- `9a1446a` / `ar5090-20260319-182232`: widen the accepted balanced `4 shared x 2 loops` backbone from `mlp_mult=2` to `mlp_mult=3`. Lost clearly at `val_bpb=1.734052179` despite using more bytes (`artifact_bytes=9701691`) and more params (`15.32M`); avoid straight blanket MLP widening on this backbone.
- `868835d` / `ar5090-20260319-185407`: widen the accepted `4 shared x 2 loops` + rank-8 full-target adapter stack from `d_model=512` to `d_model=576`. Lost badly at `val_bpb=1.760640238` despite landing inside the target band (`artifact_bytes=10948067`); avoid blunt width increases on this backbone under the 5-minute 5090 budget.
- `d10655d` / `ar5090-20260319-191022`: raise the accepted `4 shared x 2 loops` + full-target adapter stack from `adapter_rank=8` / `adapter_alpha=16` to `adapter_rank=12` / `adapter_alpha=24`. Lost clearly at `val_bpb=1.730365120` with `artifact_bytes=9978164`; avoid simple higher-rank adapter inflation on this backbone under the 5-minute budget.
- `0242c66` / `ar5090-20260319-192726`: move the accepted short-budget fake-quant transition from `fake_quant_start_step=50` to the warmup boundary at `20`. This nudged telemetry to `val_bpb=1.711364843` with essentially unchanged throughput and bytes (`artifact_bytes=9187520`), but the `0.000311` gain was below the session's meaningful-win threshold, so revert it as noise.
- `1f71b0c` / `ar5090-20260319-194249`: add a config-visible recurrent MLP hidden bonus of `+256` on the accepted `4 shared x 2 loops` + rank-8 full-target adapter stack. Lost clearly at `val_bpb=1.725805341` while increasing bytes (`artifact_bytes=9865355`), params (`13.58M`), and peak VRAM (`20.32 GB`); avoid matching the stem/tail hidden bonus across all shared blocks under the 5-minute 5090 budget.
- `419366f` / `ar5090-20260319-195849`: attempt to move `fake_quant_start_step` from `50` to `0` by changing the dataclass default only. The wrapper still loaded `fake_quant_start_step=50` from the 5090 config JSON, so this run reused the accepted config hash (`92d607c9...`) and regressed to `val_bpb=1.718219009`; do not repeat default-only fake-quant timing edits under this harness.
- `929e082` / `ar5090-20260319-210100`: keep the wider stem MLP but taper the inherited deep-tail hidden bonus from `d_model//2` to `d_model//4` once `tail_layers>=5`. This pulled `artifact_bytes` back under the soft band to `11809971`, reduced params to `18.35M`, and slightly improved throughput / VRAM, but `val_bpb` regressed to `1.680721206` versus the accepted `1.679657444`, just beyond the session's `~0.001` meaningful-loss threshold; keep the full tail bonus on the current `1x2 + tail=7` stack.
- `9797c92` / `ar5090-20260319-214711`: extend `warmdown_steps` from `100` to `140` on the accepted `1 shared x 2 loops` + `tail_layers=7` + `shared_mlp_hidden_bonus=128` stack. The run succeeded but matched the accepted telemetry exactly at `val_bpb=1.674878955`, with slightly larger `artifact_bytes=12463413` from extra code bytes only. `config.json` confirmed the warmdown change, but `metrics.jsonl` kept `lr_scale=1.0` through the last logged step because the scheduler keys off `iterations=200000` while the 5-minute wrapper stops around step `316`; treat longer-warmdown-only probes as inert under this harness unless the schedule is made wallclock-aware or the effective iteration budget is shortened.
- `13a6992` / `ar5090-20260319-220211`: raise the accepted deep-tail stack's `shared_mlp_hidden_bonus` from `d_model//4` to `d_model//2`, matching the lone recurrent block to the stem/tail hidden width. This increased `num_params` to `19.53M`, `artifact_bytes` to `12545071`, peak VRAM to `19115.83 MB`, and slightly slowed train/eval throughput, but `val_bpb` regressed to `1.675368100` versus the accepted `1.674878955`; keep the smaller recurrent bonus and do not assume the recurrent block wants the full non-recurrent width.
- `a5863da` / `ar5090-20260319-221908`: remove the final recurrent shared block on the accepted deep-tail stack, yielding a no-recurrence `tail_layers=9` model. The run stayed stable and was faster (`train_tokens_per_second=87084.69`, `eval_tokens_per_second=331999.47`) with lower peak VRAM (`18717.89 MB`), but it expanded to `21.52M` params, `artifact_bytes=13534766`, and regressed clearly to `val_bpb=1.679075752`; keep at least one recurrent shared block in this family.
- `5fa8d69` / `ar5090-20260319-223253`: move the accepted deep-tail stack's `fake_quant_start_step` from `20` to `0` after the recurrent-MLP-width win. The run succeeded and became the numeric telemetry best at `val_bpb=1.674480409`, but that was only about `0.000399` better than the accepted `1.674878955`, below the session's `~0.001` meaningful-win threshold. It also slightly increased `artifact_bytes` to `12463527` and peak VRAM to `19034.51 MB`, so revert it as noise rather than promoting more aggressive step-0 QAT.
- `1462f60` / `ar5090-20260319-224752`: lower `matrix_lr` from `0.012` to `0.010` on the accepted `1 shared x 2 loops` + `tail_layers=7` + `shared_mlp_hidden_bonus=128` stack. This pulled `artifact_bytes` down to `12026095` and kept throughput / peak VRAM essentially flat, but `val_bpb` regressed clearly to `1.680757494`; avoid simple lower-matrix-LR-only cuts on this stack.
- `62471b3` / `ar5090-20260319-233902`: raise `muon_momentum` from `0.95` to `0.975` on the accepted `1 shared x 2 loops` + `tail_layers=7` + `shared_mlp_hidden_bonus=128` + wallclock-aware schedule + `clip_percentile=96.5` stack. This lost badly at `val_bpb=1.698826885` versus the accepted `1.656803388`, while leaving params / throughput / peak VRAM essentially unchanged and increasing `artifact_bytes` to `12776779`; avoid higher-Muon-momentum-only pushes on this active schedule.
- `05f6755` / `ar5090-20260320-073422`: extend `warmdown_steps` from `100` to `140` on the last fully measured wallclock-aware deep-tail stack (`shared_mlp_hidden_bonus=192`, `fake_quant_start_step=20`, `clip_percentile=96.5`). This run never reached training: the wrapper aborted because no files matched `TRAIN_PATTERN=/workspace/autoresearch-parameter-golf-ralph/data/datasets/fineweb10B_sp1024/fineweb_train_*.bin`, so no `results.json` was produced and `latest.json` / `best.json` stayed on `ar5090-20260319-235350`. The experiment commit was reverted for state hygiene only; re-test this hypothesis only after restoring the FineWeb shards on the host.
- `80436cb` / `ar5090-20260320-074025`: keep `tok_emb.weight` in float export passthrough on the accepted wallclock-aware deep-tail stack to probe the tied-embedding precision-policy axis. This run also never reached training: the wrapper aborted because no files matched `TRAIN_PATTERN=/workspace/autoresearch-parameter-golf-ralph/data/datasets/fineweb10B_sp1024/fineweb_train_*.bin`, so no `results.json` or concrete run directory was produced and `latest.json` / `best.json` stayed on `ar5090-20260319-235350`. The experiment commit was reverted for state hygiene only; re-test this precision-policy hypothesis only after restoring the FineWeb shards on the host.
- `3068ca5` / `ar5090-20260320-075109`: extend `warmdown_steps` from `100` to `140` on the current wider wallclock-aware deep-tail stack (`shared_mlp_hidden_bonus=224`, `fake_quant_start_step=20`, `clip_percentile=96.5`). The run completed successfully and pulled `artifact_bytes` down to `11896832`, but `val_bpb` regressed clearly to `1.663273599` versus the measured `1.653523636` reference, with slightly slower train/eval throughput and slightly higher peak VRAM. The longer active warmdown does not rescue the wider `7/16` recurrent MLP; keep `warmdown_steps=100` on this direction.
- `12a4beb` / `ar5090-20260320-080910`: widen the current wallclock-aware deep-tail recurrent MLP from `7*d_model//16` to `15*d_model//32` (`224 -> 240` hidden bonus) to test whether the lone shared block still had headroom short of the earlier full-width loser. This run never reached training: `bash scripts/run_autoresearch_experiment.sh` aborted before creating a concrete run directory because the wrapper still resolved `TRAIN_PATTERN=/workspace/autoresearch-parameter-golf-ralph/data/datasets/fineweb10B_sp1024/fineweb_train_*.bin`, while this host's FineWeb shards currently live under `/workspace/autoresearch-parameter-golf/data/datasets/fineweb10B_sp1024/`. No `results.json` was produced, `latest.json` / `best.json` remained unchanged, and the experiment commit was reverted for state hygiene. Do not spend another training iteration until the repo-local `./data` path is restored or bridged to the actual shard location.
- `eeaf92a` / `ar5090-20260320-081903`: keep `tok_emb.weight` in float export passthrough on the current wider wallclock-aware deep-tail stack (`shared_mlp_hidden_bonus=224`, `fake_quant_start_step=20`, `clip_percentile=96.5`) while overriding the wrapper's shard/tokenizer paths to the sibling repo location so the run could execute. The run completed successfully, but `val_bpb=1.655477002` still lost to the indexed best `1.653523636`, and `artifact_bytes=12021406` also landed slightly above the accepted `12018203`. Treat tied-embedding float passthrough as a losing precision-policy probe on this wider `7/16` stack under the 5-minute 5090 budget.
- `06341af` / `ar5090-20260320-083256`: retry widening the current wallclock-aware deep-tail recurrent MLP from `7*d_model//16` (`224`) to `15*d_model//32` (`240`) while overriding the wrapper's shard/tokenizer paths to the sibling repo location so the run could execute. The run completed successfully, but `val_bpb=1.655341051` regressed clearly versus the accepted `1.653523636`, while `artifact_bytes` also rose to `12023973`, `num_params` to `19.52M`, and peak VRAM to `19109.26 MB`. Keep the accepted `224` recurrent hidden bonus; do not push this lone shared block wider without reclaiming bytes elsewhere.
- `d3a1d74` / `ar5090-20260320-084932`: pair an earlier fake-quant start (`20 -> 10`) with a slightly tighter export clip (`96.5 -> 96.0`) on the current wider wallclock-aware deep-tail stack (`shared_mlp_hidden_bonus=224`) to see whether modest train/export quantization alignment could reclaim bytes without giving back quality. The run completed successfully and pulled `artifact_bytes` down to `11990449`, back under the soft `12M` target band, with essentially unchanged throughput and peak VRAM, but `val_bpb=1.655963379` regressed clearly versus the accepted `1.653523636`. Do not revisit this exact timing+clip pair on the `7/16` stack; if the quantization axis is revisited, it likely needs a more faithful train-time clipping change rather than just earlier fake quant plus a slightly tighter export percentile.
- `d05f3d8` / `ar5090-20260320-090518`: rebalance the accepted wider wallclock-aware deep-tail stack from `1 shared x 2 loops + tail_layers=7` to `1 shared x 3 loops + tail_layers=6`, holding effective depth at 10 while keeping `shared_mlp_hidden_bonus=224`, `fake_quant_start_step=20`, and `clip_percentile=96.5` fixed. This saved substantial bytes (`artifact_bytes=10960823`) and reduced params to `17.46M`, but `val_bpb=1.663226085` regressed badly versus the accepted `1.653523636`, while train/eval throughput also slipped and peak VRAM rose to `19274.60 MB`. On this deep-tail family, do not trade away a unique tail layer just to add a third pass through the lone shared block; keep the accepted `1x2 + tail=7` allocation if this axis is revisited.
- `dbb7f48` / `ar5090-20260320-092331`: store float passthrough export tensors at `keep_float_store_dtype` and restore their original dtypes on load, so adapter / norm / scale / gain tensors no longer force float32 storage in the artifact. The run completed successfully and cut `artifact_bytes` to `11768383` with `quant_payload_bytes=19696208`, while train/eval throughput and peak VRAM stayed near the prior wider-stack probes, but `val_bpb=1.655477002` still sat about `0.00195` above the measured `1.653523636` reference. Revert it: export-only byte savings do not rescue the current `7/16` recurrent-MLP stack on the 5-minute 5090 budget.
- `9e3fce9` / `ar5090-20260320-094005`: retune the current wider wallclock-aware deep-tail recurrent MLP from `7*d_model//16` (`224`) down to the midpoint `13*d_model//32` (`208`) while keeping `adapter_rank=8`, `fake_quant_start_step=20`, and `clip_percentile=96.5` fixed. The run completed successfully and landed just under the soft 12M band at `artifact_bytes=12008545`, but `val_bpb=1.655907074` still regressed clearly versus the measured `1.653523636` reference, with slightly slower train/eval throughput and slightly higher peak VRAM. Treat this midpoint retune as another losing recurrent-width probe on the current deep-tail line; if the axis is revisited again, look beyond simple hidden-bonus interpolation around the old `3/8` winner.
- `07033e2` / `ar5090-20260320-095533`: restore the branch's unmeasured `7*d_model//16` recurrent-width drift back to the last measured `3*d_model//8` anchor (`shared_mlp_hidden_bonus: 224 -> 192`) and rerun it with the working sibling-repo shard/tokenizer overrides. The run reproduced the historical best config hash `3aa4c3d7...`, trimmed `artifact_bytes` slightly to `12003847`, and matched throughput / VRAM, but it stopped at `315` steps instead of `316` and landed at `val_bpb=1.654997438`, about `0.00147` worse than the historical `1.653523636` best, so it was reverted. Treat this as a noisy exact-config replay rather than fresh evidence that `3/8` is weak; if this width axis is revisited, compare the current `7/16` tip against the `3/8` anchor with a direct measured pair instead of assuming the old best run will reproduce exactly.
- `c63232a` / `ar5090-20260320-101300`: on the current branch-tip `7*d_model//16` deep-tail stack, match the stem/tail non-recurrent MLP hidden bonus down from `d_model//2` (`256`) to `7*d_model//16` (`224`) so all blocks use the same widened MLP width and the artifact falls back under the soft 12M ceiling. The run completed successfully and did cut `artifact_bytes` to `11885776`, `num_params` to `19.24M`, and peak VRAM slightly to `19040.78 MB`, with essentially unchanged train/eval throughput, but `val_bpb=1.656877635` regressed clearly versus the measured `1.653523636` reference. Revert it: the widened deep-tail line does not want this stem/tail byte reclamation, so if byte-trimming is revisited on the `7/16` stack, look somewhere other than non-recurrent MLP width.
- `1f2d3b8` / `ar5090-20260320-103214`: reallocate the current branch-tip `7*d_model//16` deep-tail stack's extra recurrent MLP width back to the measured `3*d_model//8` anchor (`224 -> 192`) while raising full-target adapter capacity from `adapter_rank=8` / `adapter_alpha=16` to `10` / `20` so total params stayed almost flat. The run completed successfully, but `val_bpb=1.657708205` regressed clearly versus both the measured `1.653523636` anchor and the recent `3/8` replay, while `artifact_bytes` also rose to `12102874`, `num_params` to `19.49M`, and peak VRAM to `19079.59 MB`. Revert it: on this deep-tail line, shifting bytes from the recurrent MLP into slightly larger adapters is not a good trade.
- `d772dd0` / `ar5090-20260320-105228`: align the current branch-tip `7*d_model//16` deep-tail stack's train-time fake quantization clip to the export percentile (`fake_quant_clip_percentile: 100.0 -> 96.5`) while keeping `fake_quant_start_step=20` and export `clip_percentile=96.5`. The run completed successfully and did pull `artifact_bytes` down under the soft target band to `11987026`, but `val_bpb=1.672170650` collapsed far past the measured `1.653523636` reference, with slower training throughput (`81887.19 tok/s`) and slightly higher peak VRAM (`19095.65 MB`). Revert it decisively: full train-time percentile clipping is much too aggressive on this widened deep-tail line; if quantization alignment is revisited, use a much weaker or more localized train-time clipping scheme instead of matching export clipping directly.
- `db53b0f` / `ar5090-20260320-111004`: on the current branch-tip `7*d_model//16` deep-tail stack, lower `matrix_lr` from `0.012` to `0.011` and raise `muon_momentum` from `0.95` to `0.96` to test whether a gentler fixed-budget optimizer schedule helps the wider recurrent MLP. The wrapper aborted before creating a concrete run directory because no training shards matched the repo-local `./data/datasets/fineweb10B_sp1024/fineweb_train_*.bin` path in this workspace, so no `results.json` was produced and `latest.json` / `best.json` stayed unchanged. The experiment commit was reverted for state hygiene; do not spend another default-path iteration from this repo clone until the repo-local FineWeb path is restored or the wrapper is given the working shard/tokenizer overrides.
- `359fab8` / `ar5090-20260320-111652`: rerun the wider `7*d_model//16` deep-tail optimizer retune with the working sibling-repo shard/tokenizer overrides, again lowering `matrix_lr` from `0.012` to `0.011` and raising `muon_momentum` from `0.95` to `0.96`. The run completed successfully, so the earlier path failure is no longer an excuse, but it regressed badly to `val_bpb=1.669379405` versus the measured `1.653523636` reference while also increasing `artifact_bytes` to `12088650`, `num_params` to `19.50M`, and peak VRAM to `19094.50 MB`. Revert it decisively: this gentler LR plus slightly higher Muon momentum schedule does not help the widened `7/16` recurrent-MLP line under the 5-minute 5090 budget.
- `07f866b` / `ar5090-20260320-113523`: extend the current wider wallclock-aware deep-tail stack (`1 shared x 2 loops`, `shared_mlp_hidden_bonus=224`, `fake_quant_start_step=20`, `clip_percentile=96.5`) with one more unique tail block (`tail_layers: 7 -> 8`) while using the working sibling-repo shard/tokenizer overrides. The run completed successfully, but `val_bpb=1.672425563` regressed badly versus the measured `1.653523636` reference while `artifact_bytes` jumped to `13118012`, `num_params` to `21.60M`, training slowed to `73850.48 tok/s`, eval to `294371.93 tok/s`, peak VRAM to `20839.10 MB`, and the 300-second wrapper stopped at only `288` steps. Revert it: on the widened `7/16` deep-tail line, pushing unique tail depth past `7` is too expensive under the 5-minute 5090 budget unless bytes or step cost are reclaimed elsewhere.
- `8018115` / `ar5090-20260320-120900`: narrow the current wider deep-tail stack's GQA from `num_kv_heads=4` to `2` to reclaim repeated attention bytes/compute without changing `num_heads=8`. The run never reached training: `bash scripts/run_autoresearch_experiment.sh --train_pattern ... --val_pattern ... --tokenizer_path ...` still resolved the repo-local `CONFIG_JSON` paths (`./data/datasets/...`, `./data/tokenizers/...`) before those forwarded CLI overrides took effect, so no concrete run directory or `results.json` was created and both `latest.json` / `best.json` stayed unchanged. The experiment commit was reverted for state hygiene. From this clone, do not assume wrapper CLI path overrides are sufficient; either restore a repo-local `./data` bridge or run the wrapper with a config JSON whose dataset/tokenizer paths already point at the sibling repo.
- `c91c6b9` / `ar5090-20260320-120530`: retry the narrower-GQA probe on the current wider deep-tail stack with working sibling-repo `TRAIN_PATTERN` / `VAL_PATTERN` / `TOKENIZER_PATH` overrides, changing `num_kv_heads: 4 -> 2` while keeping `num_heads=8`, `shared_mlp_hidden_bonus=224`, `fake_quant_start_step=20`, and `clip_percentile=96.5` fixed. The run completed successfully and did improve throughput (`87496.66 tok/s` train, `333190.76 tok/s` eval) while reducing params to `18.32M` and peak VRAM to `18842.30 MB`, but `val_bpb=1.659256568` still regressed clearly versus the measured `1.653523636` reference and `artifact_bytes` also rose to `12772111`. Revert it: on this widened deep-tail line, halving KV heads buys speed but gives back too much quality and does not solve artifact pressure.
- `dd1b503` / `ar5090-20260320-122317`: advance the current wider deep-tail stack's fake-quant start from step `20` to step `10` while keeping `shared_mlp_hidden_bonus=224`, `num_kv_heads=4`, and export `clip_percentile=96.5` fixed. The run completed successfully and shaved `artifact_bytes` slightly to `12016613`, but `val_bpb=1.655963379` still regressed clearly versus the measured `1.653523636` reference, with slightly slower training (`83798.93 tok/s`) and a small peak-VRAM increase (`19094.50 MB`). Revert it: this exactly matched the earlier `step 10 + clip 96.0` run's `val_bpb`, so the loss comes from advancing QAT timing itself rather than from the tighter export clip.
- `e72d689` / `ar5090-20260320-124136`: on the current wider deep-tail stack (`shared_mlp_hidden_bonus=224`, `fake_quant_start_step=20`, `clip_percentile=96.5`), raise attention head granularity from `num_heads=8` / `num_kv_heads=4` to `16/8` so total Q/K/V projection widths stay nearly unchanged while each head gets `32` dims instead of `64`. This stayed under the artifact target band at `artifact_bytes=11575203`, but it was a catastrophic training-budget fit: the 300-second wallclock stop hit at only `240` steps, training throughput fell to `55360.93 tok/s`, eval to `231480.87 tok/s`, peak VRAM jumped to `25763.43 MB`, and `val_bpb` collapsed to `1.806685365`. Revert it decisively: finer head granularity is not a free structural change on this 5090 deep-tail line; it blows up attention cost even when nominal parameter bytes stay about flat.
- `c2a5c3d` / `ar5090-20260320-130049`: push the current wider deep-tail stack's lone recurrent block from the branch-tip `shared_mlp_hidden_bonus=224` all the way to a true 3x MLP (`shared_mlp_hidden_bonus: 224 -> 512`) while keeping `fake_quant_start_step=20`, `clip_percentile=96.5`, and the `1 shared x 2 loops + tail_layers=7` layout fixed. The run completed successfully and still fit the 300-second budget at `312` steps, but `val_bpb=1.657026820` regressed clearly versus the measured `1.653523636` reference while `artifact_bytes` rose to `12194001`, `num_params` to `19.80M`, training slowed to `82782.35 tok/s`, eval to `319931.64 tok/s`, and peak VRAM rose to `19280.22 MB`. Revert it: on the widened `7/16` deep-tail line, a full jump to a true 3x recurrent MLP is too expensive for the 5-minute 5090 budget; if the MLP axis is revisited, it should probably reclaim bytes elsewhere or use a cleaner multiplier change rather than another larger hidden-bonus leap.
- `e6f6b8d` / `ar5090-20260320-133515`: restore the accepted branch-tip recurrent MLP width from `7*d_model//16` back to the historical `3*d_model//8` anchor while keeping the current float-passthrough export storage fix, to get a direct measured pair against the accepted `7/16` tip instead of relying on older telemetry. The run completed successfully at the full `316`-step budget and did improve the accepted tip numerically to `val_bpb=1.655056412` while trimming `artifact_bytes` to `11748549`, `num_params` to `19.47M`, and train/eval throughput slightly up, but the gain over the accepted `1.655477002` state was only about `0.000421`, well below the session's `~0.001` meaningful-win threshold. Revert it: treat this clean `7/16` vs `3/8` replay as sub-threshold noise rather than a real winner, and do not spend another plain recurrent-width replay on this axis unless it is paired with a fresh orthogonal change.
- `6261d10` / `ar5090-20260320-135242`: delay the wallclock schedule calibration window from `max(32, 2*warmup_steps)` to `max(80, 4*warmup_steps)` so startup overhead cannot pull `lr_total_steps` down and start cosine decay too early on the current deep-tail line. The wrapper aborted before creating a concrete run directory because no training shards matched the repo-local `./data/datasets/fineweb10B_sp1024/fineweb_train_*.bin` path in this clone, so no `results.json` was produced and `latest.json` / `best.json` stayed unchanged. Revert it for state hygiene only: the schedule-calibration hypothesis remains unmeasured, and should not be retried from this repo clone until the repo-local `./data` bridge is restored or the wrapper config points at the working sibling-repo shard paths.
- `8666f79` / `ar5090-20260320-140012`: retry the delayed wallclock schedule calibration window on the accepted float-export `7*d_model//16` deep-tail line while overriding `TRAIN_PATTERN`, `VAL_PATTERN`, and `TOKENIZER_PATH` to the sibling repo so the wrapper could actually execute. This won clearly versus the accepted `1.655477002` state at `val_bpb=1.653037209` (about `0.00244` better) while keeping `artifact_bytes=11806317` inside the soft 12M band, train/eval throughput essentially flat, and peak VRAM unchanged at `19094.50 MB`. `metrics.jsonl` shows `lr_total_steps=310` instead of `303`, so the cosine tail started a bit later and stayed at a higher LR deeper into the 5-minute budget; keep this later calibration window as the optimizer-schedule baseline on the current deep-tail line.
- `836dddf` / `ar5090-20260320-142100`: enable `use_lawa` with `lawa_last_n_steps=16` on the accepted later-calibrated `7*d_model//16` deep-tail line while keeping the model and optimizer otherwise fixed. The plain final eval reproduced the accepted baseline exactly at `val_bpb=1.653037209`, but the separate `phase="lawa"` eval degraded to `val_bpb=1.656430490` at the same `315` steps, with `artifact_bytes=11804342` and unchanged peak VRAM. Revert it: a short trailing LAWA pass is a measured loser on this current deep-tail schedule, so do not revisit it on this line without a materially different averaging scheme.
- `283f905` / `ar5090-20260320-144056`: delay the accepted later-calibrated `7*d_model//16` deep-tail line's fake-quant start from `20` to the intermediate `32` while keeping the model, schedule, and export clip fixed. The run completed successfully with virtually identical throughput, steps, params, and peak VRAM, and it even trimmed `artifact_bytes` slightly to `11805662`, but `val_bpb=1.653039291` was still a hair worse than the accepted `1.653037209` and far below the session's meaningful-win threshold. Revert it: plain later QAT handoff on this line looks flat-to-worse, so keep `fake_quant_start_step=20` unless the timing axis is revisited together with a different quantization change.
- `f5997df` / `ar5090-20260320-150056`: add a weaker train-time fake-quant clipping path on the accepted later-calibrated `7*d_model//16` deep-tail line and set `fake_quant_clip_percentile: 100.0 -> 99.5` while keeping `fake_quant_start_step=20` and export `clip_percentile=96.5` fixed. The run completed successfully and trimmed `artifact_bytes` to `11766507`, but training slowed to `81555.68 tok/s`, the wallclock-calibrated schedule only reached `lr_total_steps=303` and `307` steps, and `val_bpb=1.659908524` regressed badly versus the accepted `1.653037209`. Revert it decisively: even a much weaker global train-time clipping change still damages this deep-tail line, so if quantization alignment is revisited again it should be more localized than a blanket per-weight percentile clip.
- `ce44400` / `ar5090-20260320-151939`: rebalance the accepted later-calibrated deep-tail line's MLP bytes toward recurrence by raising `shared_mlp_hidden_bonus: 224 -> 256` while introducing `non_recurrent_mlp_hidden_bonus=240`, so the repeated shared block gets a little wider and the unique stem/tail blocks get slightly slimmer. The run completed successfully with near-identical throughput, steps, and peak VRAM, and it did trim `artifact_bytes` to `11761605` and `num_params` to `19.40M`, but `val_bpb=1.656174724` regressed clearly versus the accepted `1.653037209`. Revert it: on this later-calibrated `1x2 + tail=7` line, shifting a small amount of MLP width out of the unique non-recurrent blocks and into the repeated shared block hurts quality.
- `269dc19` / `ar5090-20260320-153814`: keep the accepted later-calibrated `7*d_model//16` deep-tail line's recurrent block fixed at `shared_mlp_hidden_bonus=224`, but raise the unique stem/tail MLP hidden bonus from `d_model//2` (`256`) to `17*d_model//32` (`272`) so the extra bytes stay in non-recurrent capacity instead of moving back into recurrence. The run completed successfully, but `val_bpb=1.654084057` still regressed versus the accepted `1.653037209` while `artifact_bytes` rose to `11870886`, `num_params` to `19.63M`, peak VRAM to `19129.09 MB`, and the wallclock stop landed at only `313` steps with `lr_total_steps=308`. Revert it: this later-calibrated deep-tail line does not seem to want even a small extra non-recurrent MLP width bump at the current 5-minute 5090 budget, so future structural probes should look beyond simply adding more unique MLP width on top of the accepted `1x2 + tail=7` stack.
- `0997174` / `ar5090-20260320-155609`: double the accepted later-calibrated deep-tail line's per-loop full-target adapter capacity from `adapter_rank=8` / `adapter_alpha=16` to `16` / `32` while keeping the `1 shared x 2 loops + tail_layers=7`, `shared_mlp_hidden_bonus=224`, `fake_quant_start_step=20`, and `clip_percentile=96.5` stack fixed. The run completed successfully and still stayed just under the soft 12M band at `artifact_bytes=11998877`, but `val_bpb=1.659961696` regressed badly versus the accepted `1.653037209`, with slightly fewer steps (`314`), slightly slower train/eval throughput, and a small peak-VRAM increase (`19102.45 MB`). Revert it decisively: on the current later-calibrated deep-tail line, pure full-target adapter-rank inflation is not a good use of bytes, so if the adapter axis is revisited, prefer target reallocation or a materially different shared/non-shared layout over simply making the loop adapters larger.
- `b68d448` / `ar5090-20260320-161514`: on the accepted later-calibrated `7*d_model//16` deep-tail line, drop `k` from the lone shared block's loop-adapter targets and raise the remaining targets from `adapter_rank=8` / `adapter_alpha=16` to `12` / `24` on `{q, v, attn_out, mlp_in, mlp_out}` so the repeated adapter bytes move away from GQA keys and into the other projections. The run completed successfully and stayed inside the soft 12M band at `artifact_bytes=11858800`, with slightly lower peak VRAM (`19032.44 MB`) and slightly faster eval, but `val_bpb=1.663073239` regressed badly versus the accepted `1.653037209`, with one fewer step (`314`) and slightly slower train throughput. Revert it decisively: on this current later-calibrated deep-tail line, simple loop-adapter target pruning plus higher rank on the remaining targets is a clear loser, so future adapter work should look beyond lone-block target reallocation.
- `d1c3e11` / `ar5090-20260320-163227`: on the accepted later-calibrated `7*d_model//16` deep-tail line, move the lone non-recurrent stem block to the tail (`stem_layers: 1 -> 0`, `tail_layers: 7 -> 8`) while keeping effective depth, params, optimizer, quantization, and the lone `1 shared x 2 loops` recurrent core otherwise fixed. This won cleanly at `val_bpb=1.652025748` (about `0.00101` better than the accepted `1.653037209`) with `artifact_bytes=11819154`, the same `19.50M` params, the same `315` steps, and essentially unchanged train/eval throughput. Keep it: on this current deep-tail family, the placement of unique non-recurrent capacity still matters even when bytes stay flat, and pushing that last block later is a valid structural improvement.
- `0dac613` / `ar5090-20260320-165236`: shorten the accepted stemless deep-tail line's active cosine tail from `warmdown_steps: 100 -> 80` to keep LR higher a bit longer after the delayed wallclock calibration win. The run completed successfully and even became the numeric `best.json` telemetry at `val_bpb=1.651936372`, but that is only about `0.000089` better than the accepted `1.652025748`, well below the session's `~0.001` meaningful-win threshold, while `artifact_bytes` also rose slightly to `11872679` and the schedule estimate slipped from `lr_total_steps=310` to `309`. Revert it: this shorter warmdown is noise on the current `stem=0, 1x2, tail=8` line, so keep `warmdown_steps=100` as the accepted schedule baseline.
- `02aeb8d` / `ar5090-20260320-171052`: on the accepted stemless deep-tail line, replace the current `mlp_mult=2` plus hidden-bonus stack with a cleaner global `mlp_mult=3` and zero extra MLP bonuses (via an explicit `non_recurrent_mlp_hidden_bonus=0`) to test whether the shifted `1 shared x 2 loops + tail_layers=8` family wants a simpler ~`3x` MLP ratio. The wrapper started but aborted before creating a concrete run directory because this workspace still lacks the repo-local FineWeb shards at `./data/datasets/fineweb10B_sp1024/...`, so no `results.json` was produced and `latest.json` / `best.json` stayed unchanged on `ar5090-20260320-165236`. Revert it for state hygiene only: this structural `mlp_mult` hypothesis remains unmeasured from this clone, and should only be retried once the repo-local `./data` bridge is restored or the wrapper config already points at the working shard/tokenizer paths.
- `9da23ed` / `ar5090-20260320-171749`: rerun the same clean shifted-deep-tail `mlp_mult=3` probe from this clone, but this time launch the wrapper with the working sibling-repo shard and tokenizer overrides so the experiment actually executes. The run completed successfully, so this axis is now measured on the accepted `stem=0, 1 shared x 2 loops, tail=8` line, and it lost clearly at `val_bpb=1.660239586` versus the accepted `1.652025748` while raising `artifact_bytes` to `13056266`, params to `21.90M`, peak VRAM to `19759.97 MB`, and slowing the wallclock stop to `304` steps. Revert it decisively: a clean global `3x` MLP overspends bytes and compute on this current stemless deep-tail line, so if `mlp_mult` is revisited again it should come with explicit byte reclamation elsewhere rather than a blanket widening.
- `6e4f2ff` / `ar5090-20260320-173323`: keep `tok_emb.weight` in float export passthrough on the accepted stemless deep-tail line (`stem=0, 1 shared x 2 loops, tail=8`, `shared_mlp_hidden_bonus=224`, `fake_quant_start_step=20`, `clip_percentile=96.5`) to re-check the tied-embedding precision-policy axis after the later stem-to-tail win. The wrapper aborted before creating a concrete run directory because this clone still has no repo-local FineWeb shards at `./data/datasets/fineweb10B_sp1024/...`, so there is no `results.json`, no `crash.json`, and `latest.json` / `best.json` remained unchanged on the prior `ar5090-20260320-171749` telemetry. Revert it for state hygiene only: this stemless tied-embedding float-export hypothesis remains unmeasured from this clone and should only be retried once the repo-local `./data` bridge is restored or the wrapper config already points at the working sibling-repo shard/tokenizer paths.
- `935ff4f` / `ar5090-20260320-174135`: on the accepted stemless deep-tail line, retune the lone recurrent MLP from `7*d_model//16` (`224`) back to the older `3*d_model//8` (`192`) while keeping the delayed wallclock schedule, `fake_quant_start_step=20`, and export `clip_percentile=96.5` fixed. The run completed successfully and even became numeric `best.json` telemetry at `val_bpb=1.651376583`, with slightly lower `artifact_bytes=11804832`, `num_params=19.47M`, and essentially unchanged throughput / peak VRAM versus the accepted `1.652025748` stemless baseline. Revert it: the gain is only about `0.000649`, still below the session's `~0.001` meaningful-win threshold, so treat this stemless `7/16` vs `3/8` replay as another sub-threshold width probe rather than a true promotion.
- `78b3822` / `ar5090-20260320-175808`: rerun the stemless tied-embedding float-export probe on the accepted `stem=0, 1 shared x 2 loops, tail=8` line, this time with the sibling-repo shard and tokenizer overrides so the wrapper could actually execute. The run completed successfully and kept `artifact_bytes` effectively flat at `11819183`, but `val_bpb=1.652531510` regressed versus the accepted `1.652025748`, with one fewer step (`314` vs `315`), slightly slower train/eval throughput, and a small peak-VRAM increase (`19098.00 MB`). Revert it: on the current stemless deep-tail line, plain `tok_emb.weight` float passthrough is now a measured losing precision-policy probe, so deprioritize this exact tied-embedding export change unless it is paired with a materially different quantization or architecture hypothesis.
- `a0f713a` / `ar5090-20260320-181525`: on the accepted stemless deep-tail line, combine the earlier sub-threshold `3/8` recurrent-width replay with the earlier shorter-cosine-tail signal by setting `shared_mlp_hidden_bonus: 224 -> 192` and `warmdown_steps: 100 -> 80`. This won clearly at `val_bpb=1.650638476` (about `0.00139` better than the accepted `1.652025748`) while keeping `artifact_bytes=11860072` inside the soft 12M band, `num_params=19.47M`, the full `315` steps, and essentially unchanged train/eval throughput plus peak VRAM. Keep it: on the current `stem=0, 1x2, tail=8` line, the narrower recurrent MLP becomes a real winner once the cosine tail is shortened slightly, so future schedule or MLP probes should build on this paired baseline rather than on the older `7/16` + `warmdown_steps=100` state.
- `3be89c0` / `ar5090-20260320-183241`: on the accepted stemless deep-tail line, lift `seq_len` from `512` to `1024` as a temporary structural-jump probe to test whether a longer context window is the next production-relevant gain on top of the current `1x2 + tail=8`, `shared_mlp_hidden_bonus=192`, `warmdown_steps=80` stack. The wrapper aborted before creating a run directory because this clone still has no repo-local FineWeb shards at `./data/datasets/fineweb10B_sp1024/...`, so there is no `results.json`, no `crash.json`, and `latest.json` / `best.json` remained pinned to `ar5090-20260320-181525`. Revert it for state hygiene only: this `seq_len=1024` context-length jump remains unmeasured from this clone and should only be retried once the repo-local `./data` bridge is restored or the wrapper already points at the working sibling-repo shard/tokenizer paths.

External priors:
- Strong public competitors often win by improving the baseline family rather than replacing it.
- Wider MLPs, often around 3x, appear frequently in strong runs.
- Mixed lower-bit quantization on large matrices appears promising.
- FP16 tied embedding passthrough appears promising.
- Lower LR, higher Muon momentum, and longer warmdown appear promising.
- Sliding-window evaluation appears in strong public runs, but only use it if it stays competition-valid.
- Treat these as priors, not recipes. Stay within the current baseline family and keep export/reload/accounting honest.
- Near-term priorities: `mlp_mult`, embedding/head precision policy, lower-bit matrix quantization feasibility, optimizer schedule, fake-quant timing / clip percentile.

  ## Temporary Search Mode: Structural Jump Phase

  For the next 3-5 iterations, bias strongly toward larger jumps instead of neighborhood hill-climbing.

  Instructions:
  - Prefer experiments that materially change architecture, byte allocation, context length, or export/eval behavior.
  - Do not use these iterations for tiny optimizer-only or fake-quant-only retunes.
  - Candidate directions:
    - `mlp_mult`
    - `seq_len` toward `2048`
    - more substantial `shared_layers` vs `recurrence_loops` reallocations
    - `tail_layers` only if paired with a meaningful compensating reallocation
    - embedding/head precision policy
    - lower-bit or mixed-bit export feasibility
    - evaluation-path changes only if they remain competition-valid
  - Require each jump to have a clear hypothesis, not just “make it bigger”.
  - If no clear win appears after this bounded phase, revert to the current accepted best and resume normal search.

  This is a temporary exploration phase before H100 rehearsal, not the new default mode.
  EOF

## Temporary Search Mode: Structural Jump Phase

For the next 3-5 iterations, bias strongly toward larger jumps instead of neighborhood hill-climbing.

Instructions:
- Prefer experiments that materially change architecture, byte allocation, context length, or export/eval behavior.
- Do not use these iterations for tiny optimizer-only or fake-quant-only retunes.
- Candidate directions:
- `mlp_mult`
- `seq_len` toward `2048`
- more substantial `shared_layers` vs `recurrence_loops` reallocations
- `tail_layers` only if paired with a meaningful compensating reallocation
- embedding/head precision policy
- lower-bit or mixed-bit export feasibility
- evaluation-path changes only if they remain competition-valid
- Require each jump to have a clear hypothesis, not just “make it bigger”.
- If no clear win appears after this bounded phase, revert to the current accepted best and resume normal search.

This is a temporary exploration phase before H100 rehearsal, not the new default mode.

## Temporary Search Mode: Leaderboard-Informed Structural Jump Phase

For the next 3-5 iterations, prefer larger experiments inspired by strong public patterns.

Priority ideas:
- materially wider MLP, including `MLP 3x` if feasible
- fp16 tied embedding passthrough / embedding-head precision policy
- coherent optimizer retune bundle: lower LR, higher Muon momentum, longer warmdown
- int6 or mixed low-bit quantization on large matrices if implementable cleanly
- sliding-window evaluation only if it remains competition-valid
- longer context such as `seq_len=2048` if feasible

Avoid during this temporary phase:
- tiny hidden-bonus retunes
- LR-only nudges
- clip-only nudges
- adapter-alpha-only nudges
- other neighborhood micro-tuning

This is a bounded exploration phase before H100 rehearsal, not the permanent search mode.

Latest iteration:
- `ff8dd1a` / `ar5090-20260321-035058`: rerun the previously unmeasured bounded context jump on the accepted compact `tail=4`, `6x` unique-tail MLP line (`stem=0, 1x2, tail=4, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=2048, warmdown_steps=80` + MLP-only int6 export), this time with explicit sibling-repo shard/tokenizer overrides so it actually trained. This won clearly at `val_bpb=1.591737349` versus the accepted `1.594298586`, while keeping `artifact_bytes=7,663,921` inside the target band, with `18.41M` params, `14.19 GB` peak VRAM, and a healthy fixed-budget fit (`450` steps, `124.3k` train / `430.1k` eval tok/s). Keep it as the new accepted state: on the current compact depth-6 `tail=4`, `6x` line, a moderate `seq_len: 512 -> 640` lift is now a measured production-plausible win, so treat `640` as the new context baseline and only push longer context again if another change first reclaims substantial compute.
- `bd15ebe` / `ar5090-20260321-034334`: on the accepted compact `tail=4`, `6x` unique-tail MLP line (`stem=0, 1x2, tail=4, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=2048, warmdown_steps=80` + MLP-only int6 export), try a bounded context-length jump by raising `seq_len: 512 -> 640` and retuning `train_batch_tokens=val_batch_tokens=122880` so the batch stays divisible for both 1-GPU and 8-GPU runs. This iteration never reached training: the wrapper again aborted immediately because no files matched the repo-local `TRAIN_PATTERN=/workspace/autoresearch-parameter-golf-ralph/data/datasets/fineweb10B_sp1024/fineweb_train_*.bin`, no concrete run directory, `results.json`, or `crash.json` was produced for `ar5090-20260321-034334`, and both `latest.json` and `best.json` stayed pinned to the earlier measured runs. Revert it for state hygiene only: this compact-line `seq_len=640` probe remains unmeasured from this clone until the wrapper uses the working sibling-repo shard/tokenizer paths or a repo-local `./data` bridge is restored.
- `910001c` / `ar5090-20260321-032825`: on the accepted `tail=4`, `6x` unique-tail MLP line (`stem=0, 1x2, tail=4, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=2048, warmdown_steps=80` + MLP-only int6 export), spend some of the recovered step / VRAM headroom on a modest base-width jump by setting `d_model: 512 -> 544` while preserving the measured `6x` tail ratio and the current recurrent-width ratio. The run completed successfully, but it lost decisively at `val_bpb=1.612690443` versus the accepted `1.594298586`, while increasing `artifact_bytes` to `8,251,606`, `num_params` to `20.74M`, peak VRAM to `15.06 GB`, and reducing the fixed-budget stop to `391` steps with much slower throughput (`114.8k` train / `426.0k` eval tok/s). Revert it decisively: on the current compact depth-6 `tail=4`, `6x` family, a near-neighbor `d_model` spend is still far too compute-expensive for the 5090 budget, so deprioritize further base-width inflation here unless another change first reclaims a much larger step budget.
- `d89236e` / `ar5090-20260321-031256`: on the accepted `tail=4`, `6x` unique-tail MLP line (`stem=0, 1x2, tail=4, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=2048, warmdown_steps=80` + MLP-only int6 export), raise the global MLP ratio from `mlp_mult=2` to `3` while trimming the unique-tail bonus from `2048` to `1536` so the tail blocks stay at the measured `6x` width and the repeated shared block gets the extra capacity. The run completed successfully, but it lost clearly at `val_bpb=1.600130380` versus the accepted `1.594298586`, while increasing `artifact_bytes` to `7,782,581`, `num_params` to `18.95M`, peak VRAM to `14.68 GB`, and reducing the fixed-budget stop to `435` steps with slower throughput (`131.2k` train / `462.4k` eval tok/s). Revert it decisively: on the current int6 `tail=4`, `6x` line, spending extra global MLP width on the repeated shared block does not pay back the step-budget loss, so deprioritize further near-neighbor `mlp_mult=3` reallocations in this exact neighborhood.
- `e296260` / `ar5090-20260321-030456`: on the accepted `tail=4`, `6x` unique-tail MLP line (`stem=0, 1x2, tail=4, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=2048, warmdown_steps=80` + MLP-only int6 export), raise the global MLP ratio from `mlp_mult=2` to `3` while trimming the unique-tail bonus from `2048` to `1536` so the tail blocks stay at the measured `6x` width and the repeated shared block gets the extra capacity. This iteration never reached training: the wrapper again aborted with missing repo-local FineWeb shards at `TRAIN_PATTERN=/workspace/autoresearch-parameter-golf-ralph/data/datasets/fineweb10B_sp1024/fineweb_train_*.bin`, no concrete run directory or `results.json` was created, and both `latest.json` and `best.json` stayed pinned to the earlier measured runs. Revert it for state hygiene only: this `mlp_mult=3` reallocation on the current `tail=4`, `6x` line remains unmeasured from this clone until the repo-local `./data` bridge or wrapper path plumbing is fixed again.
- `247f4e7` / `ar5090-20260321-024958`: on the accepted `tail=4`, `6x` unique-tail MLP line (`stem=0, 1x2, tail=4, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=2048, warmdown_steps=80` + MLP-only int6 export), try one more bounded width spend by keeping `tail_layers=4` fixed and widening the unique tail MLPs from `6x` to `7x` (`non_recurrent_mlp_hidden_bonus: 2048 -> 2560`). The run completed successfully, but it lost clearly at `val_bpb=1.602569052` versus the accepted `1.594298586`, while increasing `artifact_bytes` to `8,178,277`, `num_params` to `20.51M`, peak VRAM to `15.11 GB`, and reducing the fixed-budget stop to `430` steps with slower throughput (`129.4k` train / `459.3k` eval tok/s). Revert it decisively: on the current int6 `tail=4` line, another simple unique-tail width step past `6x` is now a measured loser, so shift the next structural jump away from this exact width-only ladder.
- `ab96efb` / `ar5090-20260321-023626`: rerun the previously unmeasured `tail=4`, `6x` unique-tail MLP follow-up on the accepted `tail=4`, `5x` line (`stem=0, 1x2, tail=4, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=1536, warmdown_steps=80` + MLP-only int6 export), this time with explicit sibling-repo shard/tokenizer overrides so it was actually measured. This won clearly at `val_bpb=1.594298586` versus the accepted `1.597434927`, while keeping `artifact_bytes=7,657,311` comfortably inside the target band, with `18.41M` params, `14.35 GB` peak VRAM, and a still-healthy fixed-budget fit (`445` steps, `135.1k` train / `471.8k` eval tok/s). Keep it as the new accepted state: on the current int6 `tail=4` line, there is still headroom in one more bounded unique-tail MLP width spend, and this measured `6x` winner is now the right base for the next structural jump.
- `ba67914` / `ar5090-20260321-023032`: on the accepted `tail=4`, `5x` unique-tail MLP line (`stem=0, 1x2, tail=4, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=1536, warmdown_steps=80` + MLP-only int6 export), try one more bounded width spend by keeping `tail_layers=4` fixed and widening the unique tail MLPs from `5x` to `6x` (`non_recurrent_mlp_hidden_bonus: 1536 -> 2048`). This iteration never reached training: the wrapper aborted immediately with `No training shards matched TRAIN_PATTERN=/workspace/autoresearch-parameter-golf-ralph/data/datasets/fineweb10B_sp1024/fineweb_train_*.bin`, no concrete run directory or `results.json` was created, and `latest.json` / `best.json` stayed pinned to `ar5090-20260321-021700`. Revert it for state hygiene only: this `tail=4`, `6x` follow-up remains unmeasured from this clone until the repo-local `./data` bridge or wrapper path plumbing is fixed again.
- `db356f6` / `ar5090-20260321-021700`: on the accepted `tail=4`, `4.5x` unique-tail MLP line (`stem=0, 1x2, tail=4, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=1280, warmdown_steps=80` + MLP-only int6 export), spend some of the reclaimed step / byte budget on width rather than another depth cut by keeping `tail_layers=4` and widening the unique tail MLPs from `4.5x` to `5x` (`non_recurrent_mlp_hidden_bonus: 1280 -> 1536`). This won clearly at `val_bpb=1.597434927` versus the accepted `1.600118753`, while bringing `artifact_bytes` back into the target band at `7,195,446`, with `16.31M` params, `13.59 GB` peak VRAM, and a similar fixed-budget fit (`461` steps, `141.2k` train / `485.2k` eval tok/s). Keep it as the new accepted state: on the current int6 tail-width family, there is still headroom in a bounded width spend on the surviving `tail=4` stack, and this is a better next base than continuing to shrink the model further below the target floor.
- `5e5adbf` / `ar5090-20260321-020216`: rerun the previously unmeasured `tail=4`, `4.5x` unique-tail MLP follow-up on the accepted `tail=5`, `4x` line (`stem=0, 1x2, tail=5, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=1024, warmdown_steps=80` + MLP-only int6 export), this time with explicit sibling-repo shard/tokenizer overrides so it was actually measured. This won very clearly at `val_bpb=1.600118753` versus the accepted `1.608501302`, while further cutting `artifact_bytes` to `6,905,564`, `num_params` to `15.26M`, and peak VRAM to `13.20 GB`, and raising the fixed-budget stop to `470` steps with much faster throughput (`144.5k` train / `492.2k` eval tok/s). Keep it as the new accepted state: the current depth-to-width path still has strong headroom through this `tail=4`, `4.5x` unique-tail MLP line, but the accepted artifact is now slightly below the session's soft 7M floor, so the next iteration should likely spend some of the reclaimed step/byte budget on another bounded structural axis rather than settle into micro-tuning.
- `7bd00c2` / `ar5090-20260321-015553`: on the accepted `tail=5`, `4x` unique-tail MLP line (`stem=0, 1x2, tail=5, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=1024, warmdown_steps=80` + MLP-only int6 export), continue the same depth-to-width path by trading one more unique tail block for `4.5x` unique tail MLPs (`tail_layers: 5 -> 4`, `non_recurrent_mlp_hidden_bonus: 1024 -> 1280`). This iteration never reached training: `.autoresearch/experiments.jsonl` recorded `run_start` and then `run_abort` with `reason=missing_results_json`, no concrete run directory or `results.json` was created, and `latest.json` / `best.json` stayed pinned to `ar5090-20260321-014245`. Revert it for state hygiene only: this `tail=4`, `4.5x` follow-up remains unmeasured from this clone until the repo-local `./data` bridge or wrapper path plumbing is fixed again.
- `09538f0` / `ar5090-20260321-014245`: on the accepted `tail=6`, `3.5x` unique-tail MLP line (`stem=0, 1x2, tail=6, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=768, warmdown_steps=80` + MLP-only int6 export), continue the same depth-to-width path by trading one more unique tail block for `4x` unique tail MLPs (`tail_layers: 6 -> 5`, `non_recurrent_mlp_hidden_bonus: 768 -> 1024`). This won very clearly with `val_bpb=1.608501302` versus the accepted `1.620017484`, while also cutting `artifact_bytes` to `7,643,643`, `num_params` to `17.10M`, peak VRAM to `14.76 GB`, and raising the fixed-budget stop to `412` steps with much faster throughput (`121.6k` train / `431.1k` eval tok/s). Keep it as the new accepted state: on the current int6 tail-width family, the depth-to-width trade still has strong headroom at least through this `tail=5`, `4x` unique-tail MLP line, so future follow-ups should start from this cheaper, faster `depth=7` baseline rather than the older `depth=8`, `3.5x` line.
- `e440ed2` / `ar5090-20260321-012646`: on the accepted `tail=6`, `3.5x` unique-tail MLP line (`stem=0, 1x2, tail=6, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=768, warmdown_steps=80` + MLP-only int6 export), de-share the lone recurrent pair into `2 shared x 1 loop` while keeping effective depth fixed at `8`. The run completed successfully, but it lost clearly at `val_bpb=1.622313949` versus the accepted `1.620017484`, while increasing `artifact_bytes` to `9,122,636`, `num_params` to `20.45M`, slightly lowering the fixed-budget stop to `369` steps, and nudging peak VRAM up to `16.29 GB`. Revert it decisively: on the new `3.5x` tail winner, spending bytes on a unique recurrent pair does not beat the cheaper repeated-pair baseline, so keep `1 shared x 2 loops` as the recurrent structure unless a future change pairs de-sharing with a larger orthogonal win.
- `b62e241` / `ar5090-20260321-011115`: rerun the previously unmeasured `tail=6`, `3.5x` tail-MLP hypothesis on the accepted true-`3x` tail line (`stem=0, 1x2, tail=7, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=512, warmdown_steps=80` + MLP-only int6 export), this time with explicit sibling-repo shard/tokenizer overrides so the wider-tail test actually measured. This won decisively at `val_bpb=1.620017484` versus the accepted `1.637045167`, while also reducing `artifact_bytes` to `8,229,050`, `num_params` to `18.41M`, and peak VRAM to `16.27 GB`, and increasing the fixed-budget stop to `373` steps with much faster throughput (`106.0k` train / `385.5k` eval tok/s). Keep it as the new accepted state: on the current int6 true-`3x` tail family, another depth-to-width step (`tail_layers: 7 -> 6`, `non_recurrent_mlp_hidden_bonus: 512 -> 768`, i.e. `3x -> 3.5x` unique tail MLPs) is a large production-plausible win, so future search should build on this new `depth=8` line rather than on the older deeper `tail=7` baseline.
- `a3dcb15` / `ar5090-20260321-010446`: on the accepted true-`3x` tail line (`stem=0, 1x2, tail=7, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=512, warmdown_steps=80` + MLP-only int6 export), continue the current depth-to-width trend by trading another unique tail block for wider unique MLPs (`tail_layers: 7 -> 6`, `non_recurrent_mlp_hidden_bonus: 512 -> 768`, i.e. `3x -> 3.5x` tail MLPs). This iteration never reached training: even with `CONFIG_JSON` pointed at the last accepted run's absolute-path config, `runpod_5090_train.sh` still aborted immediately with `No training shards matched TRAIN_PATTERN=/workspace/autoresearch-parameter-golf-ralph/data/datasets/fineweb10B_sp1024/fineweb_train_*.bin`, no concrete run directory or `results.json` was created, and `latest.json` / `best.json` stayed pinned to `ar5090-20260321-004852`. Revert it for state hygiene only: this `tail=6`, wider-tail-MLP hypothesis remains unmeasured from this clone until the repo-local `./data` bridge is restored or the wrapper is launched with explicit working shard/tokenizer overrides again.
- `7e5bff4` / `ar5090-20260321-004852`: rerun the previously unmeasured `shared_layers vs recurrence_loops` probe on the accepted true-`3x` tail line (`stem=0, 1x2, tail=7, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=512, warmdown_steps=80` + MLP-only int6 export), this time with explicit sibling-repo shard/tokenizer overrides so the de-shared recurrent-pair hypothesis was actually measured as `2 shared x 1 loop`. The run completed successfully and became the numeric telemetry best at `val_bpb=1.636625208`, with essentially unchanged budget fit (`339` steps, `93.1k` train / `350.4k` eval tok/s, `17.77 GB` peak VRAM), but the gain versus the accepted `1.637045167` was only about `0.000420`, below the session's `~0.001` meaningful-win threshold, while `artifact_bytes` rose to `9,505,925` and `num_params` to `21.23M`. Revert it as below-threshold noise rather than promote it: on the current true-`3x` tail family, de-sharing the lone recurrent pair looks directionally positive but too small to accept on its own, so only revisit this axis if it pairs naturally with another larger structural win.
- `563064d` / `ar5090-20260321-004245`: on the accepted true-`3x` tail line (`stem=0, 1x2, tail=7, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=512, warmdown_steps=80` + MLP-only int6 export), test the `shared_layers vs recurrence_loops` axis by de-sharing the lone recurrent pair into `2 shared x 1 loop` while keeping the winning depth-9 tail stack otherwise fixed. This iteration never reached training: the wrapper aborted immediately with `No training shards matched TRAIN_PATTERN=/workspace/autoresearch-parameter-golf-ralph/data/datasets/fineweb10B_sp1024/fineweb_train_*.bin`, no concrete run directory or `results.json` was created, and `latest.json` / `best.json` stayed pinned to the prior measured runs. Revert it for state hygiene only: this de-shared recurrent-pair hypothesis remains unmeasured from this workspace until the repo-local `./data` bridge is restored or the wrapper config again resolves to the working sibling-repo shard/tokenizer paths.
- `2381a06` / `ar5090-20260321-002541`: on the accepted true-`3x` tail line (`stem=0, 1x2, tail=7, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=512, warmdown_steps=80` + MLP-only int6 export), spend some of the recovered step / VRAM headroom on a modest base-width jump by setting `d_model: 512 -> 544` while keeping the winning tail allocation otherwise fixed. The run completed successfully and stayed inside the soft artifact band at `artifact_bytes=9,243,586`, but it lost decisively at `val_bpb=1.660749739` versus the accepted `1.637045167`, with only `297` steps instead of `340`, slower throughput (`78.6k` train / `318.8k` eval tok/s), higher peak VRAM (`18.42 GB`), and a larger model (`21.38M` params). Revert it decisively: on the current true-`3x` tail family, modest base-width inflation spends too much fixed-budget compute for the 5090 tier, so do not revisit near-neighbor `d_model` increases here unless another change first reclaims substantial step budget.
- `26dbdd3` / `ar5090-20260320-234437`: on the accepted `stem=0, 1x2, tail=8, shared_mlp_hidden_bonus=192, warmdown_steps=80` + MLP-only int6 export line, make the non-recurrent tail MLP bonus explicit and trade one unique tail block for true-`3x` unique MLPs by setting `non_recurrent_mlp_hidden_bonus: 256 -> 512` and `tail_layers: 8 -> 7`. This won very clearly at `val_bpb=1.637045167` versus the accepted `1.650194771`, while also reducing `artifact_bytes` to `8,635,451`, `num_params` to `19.20M`, and peak VRAM to `17.75 GB`, and increasing the fixed-budget stop to `340` steps with faster throughput (`93.4k` train / `350.4k` eval tok/s). Keep it: on the current int6 stemless deep-tail family, a shallower but much wider unique tail is decisively better than the deeper `tail=8` baseline, so future MLP or quantization probes should build on this new `depth=9` winner rather than on the older depth-10 layout.
- `1c3e20d` / `ar5090-20260320-232438`: on the accepted `stem=0, 1x2, tail=8, shared_mlp_hidden_bonus=192, warmdown_steps=80` + MLP-only int6 export line, change train-time MLP fake quantization to use the same 6-bit rowwise clip percentile (`96.5`) as export instead of the earlier max-abs 8-bit surrogate. The run completed successfully, but it lost decisively at `val_bpb=1.668368609` versus the accepted `1.650194771`, with fewer steps (`307` vs `316`) and slower training throughput (`81.6k` vs `84.0k` tok/s) while the exported quant payload stayed identical and `artifact_bytes` only dipped to `8,830,172`. Revert it decisively: on the current int6-MLP-export stemless deep-tail line, percentile-based train-side MLP fake-quant alignment is both slower and much worse, so do not revisit this exact clipping-alignment path without a materially cheaper implementation.
- `0f79a01` / `ar5090-20260320-230317`: lift the accepted `stem=0, 1x2, tail=8, shared_mlp_hidden_bonus=192, warmdown_steps=80` + MLP-only int6 export line from `seq_len=512` to `768`, with `train_batch_tokens=val_batch_tokens=129024` to keep the per-rank batch divisible. The run completed successfully, but it lost decisively at `val_bpb=1.671430992` versus the accepted `1.650194771`, with only `276` steps, much slower throughput (`66.8k` train / `266.6k` eval tok/s), and peak VRAM rising to `22.13 GB` even though `artifact_bytes` fell to `8,659,339`. Revert it decisively: on the current int6-MLP-export stemless deep-tail line, even an intermediate context jump to `768` is too compute-expensive for the 5-minute 5090 tier, so deprioritize longer-context probes here unless substantial compute is reclaimed first or the test moves to H100.
- `32588fa` / `ar5090-20260320-224521`: rerun the previously unmeasured current-line optimizer bundle on the accepted `stem=0, 1x2, tail=8, shared_mlp_hidden_bonus=192, warmdown_steps=80` + MLP-only int6 export stack with working sibling-repo shard/tokenizer overrides, lowering `matrix_lr: 0.012 -> 0.011`, raising `muon_momentum: 0.95 -> 0.96`, and restoring `warmdown_steps: 80 -> 100`. The run completed successfully and matched the accepted budget fit closely (`316` steps, `84.0k` train / `322.8k` eval tok/s, `19.08 GB` peak VRAM), while `artifact_bytes` only nudged down to `8,885,974`, but `val_bpb=1.665515367` regressed catastrophically versus the accepted `1.650194771`. `metrics.jsonl` still landed at `lr_total_steps=310`, so this bundle mostly lowered the late-run LR floor and started the longer decay earlier rather than extending the schedule. Revert it decisively: on the current int6-MLP-export stemless deep-tail line, this lower-LR / higher-Muon / longer-warmdown bundle is now measured and is a clear loser.
- `5d816df` / `ar5090-20260320-222959`: rerun the accepted `stem=0, 1x2, tail=8, shared_mlp_hidden_bonus=192, warmdown_steps=80` + MLP-only int6 export precision-policy probe, this time also changing export semantics so large `keep_float_name_patterns` are honored and `tok_emb.weight` really stays fp16 in the shipped artifact. The run completed successfully and did measure the intended hypothesis: `keep_float_name_patterns` in `config.json` / `manifest.json` included `tok_emb.weight`, `quant_payload_bytes` rose from `19,661,328` to `20,183,568`, and `artifact_bytes` rose to `9,508,479`, but `val_bpb=1.651069865` still lost to the accepted `1.650194771` with essentially unchanged throughput / VRAM and `315` steps. Revert it decisively: on the current int6-MLP-export stemless deep-tail line, true fp16 tied-embedding passthrough is now measured and is not a good trade, so deprioritize further embedding/head fp16 export spends here unless they are paired with a materially different low-bit mix or broader architecture change.
- `be873ac` / `ar5090-20260320-221345`: on the accepted `stem=0, 1x2, tail=8, shared_mlp_hidden_bonus=192, warmdown_steps=80` + MLP-only int6 export line, try a precision-policy spend by adding `tok_emb.weight` to `keep_float_name_patterns` so the tied embedding/head path would export in fp16. The run completed successfully, but it lost at `val_bpb=1.651231837` versus the accepted `1.650194771`, with `artifact_bytes=8,892,084`, `314` steps, and essentially unchanged throughput / VRAM. Revert it, and treat the intended hypothesis as effectively unmeasured: the exported `quant_payload_bytes` stayed identical at `19,661,328`, which exposed that the current export logic only applies `keep_float_name_patterns` on tensors at or below `keep_float_max_numel`, so this large tied embedding still quantized exactly like the baseline.
- `e64c617` / `ar5090-20260320-220701`: on the accepted `stem=0, 1x2, tail=8, shared_mlp_hidden_bonus=192, warmdown_steps=80` + MLP-only int6 export line, test a coherent optimizer bundle by lowering `matrix_lr: 0.012 -> 0.011`, raising `muon_momentum: 0.95 -> 0.96`, and restoring `warmdown_steps: 80 -> 100`. This iteration never reached training: the wrapper aborted immediately with `No training shards matched TRAIN_PATTERN=/workspace/autoresearch-parameter-golf-ralph/data/datasets/fineweb10B_sp1024/fineweb_train_*.bin`, no concrete run directory or `results.json` was created, and `latest.json` / `best.json` stayed pinned to the earlier measured runs. Revert it for state hygiene only: this optimizer-bundle hypothesis remains unmeasured from this clone until the repo-local `./data/...` bridge is restored or the wrapper is launched with working shard/tokenizer overrides again.
- `bef86f9` / `ar5090-20260320-214954`: rerun the previously unmeasured full-KV-head probe on the accepted `stem=0, 1x2, tail=8, shared_mlp_hidden_bonus=192, warmdown_steps=80` + MLP-only int6 export line, this time with explicit sibling-repo shard/tokenizer overrides so it actually trained. The run completed successfully, but it lost decisively at `val_bpb=1.665910251` versus the accepted `1.650194771`, with only `292` steps, slower throughput (`75.4k` train / `300.0k` eval tok/s), higher peak VRAM (`19.59 GB`), and larger `artifact_bytes=10,192,356`. Revert it decisively: on the current int6-MLP-export baseline, `num_kv_heads=8` overspends attention capacity and compute for the 5-minute 5090 budget, so deprioritize further full-KV-head probes in this neighborhood.
- `3a70f15` / `ar5090-20260320-210646`: keep the accepted `stem=0, 1x2, tail=8, shared_mlp_hidden_bonus=192, warmdown_steps=80` stack and current MLP-only int6 export fixed, but align train-time MLP fake quantization with export by switching `mlp_fake_quant_bits: 8 -> 6` on `mlp.fc` / `mlp.proj` only. The run completed successfully and matched the accepted budget fit closely (`315` steps, `83.8k` train / `322.6k` eval tok/s, `19.08 GB` peak VRAM), but it still lost clearly at `val_bpb=1.651342214` versus the accepted `1.650194771`, while `artifact_bytes` also ticked up slightly to `8,892,672` from the extra code bytes. Revert it decisively: on the current stemless deep-tail int6-export line, localized train/export int6 MLP fake-quant alignment is a net loser, so deprioritize this exact MLP-only train-side low-bit match and look for a materially different low-bit or optimizer hypothesis instead.
- `62af5b1` / `ar5090-20260320-204750`: spend the accepted int6-MLP-export headroom on one extra unique tail block by moving the stemless deep-tail line from `tail_layers=8` to `tail_layers=9` (`depth: 10 -> 11`) while keeping `shared_mlp_hidden_bonus=192`, `warmdown_steps=80`, and MLP-only int6 export fixed. The run completed successfully and stayed under the soft artifact ceiling at `artifact_bytes=9,665,272`, but it lost decisively at `val_bpb=1.672651912` versus the accepted `1.650194771`, with slower throughput (`73.7k` train / `294.5k` eval tok/s), only `287` steps in the fixed budget, and higher peak VRAM (`20.82 GB`). Revert it decisively: on the current int6 stemless deep-tail line, adding a full extra unique block overspends compute for the 5-minute 5090 budget and does not translate the reclaimed artifact headroom into usable quality.
- `dfc9d2b` / `ar5090-20260320-184030`: rerun the current accepted stemless deep-tail line with a structural jump from `seq_len=512` to `1024`, this time using the working sibling-repo shard/tokenizer overrides so the hypothesis was actually measured. The run completed successfully, but it lost decisively at `val_bpb=1.712032145` versus the accepted `1.650638476`, with only `237` steps in the fixed 300-second budget, much slower throughput (`54.3k` train / `228k` eval tok/s), and peak VRAM jumping to `25.74 GB`. Revert it decisively: on the current `stem=0, 1x2, tail=8, shared_mlp_hidden_bonus=192, warmdown_steps=80` line, doubling context length is far too expensive for the 5-minute 5090 tier; do not revisit longer context here without reclaiming substantial compute or moving the test to H100.
- `c8de97c` / `ar5090-20260320-190352`: test a larger `mlp_mult` jump on the current accepted stemless deep-tail line by pairing `num_kv_heads: 4 -> 2` with a clean `mlp_mult=3` conversion (`non_recurrent_mlp_hidden_bonus=0`, `shared_mlp_hidden_bonus=0`) so the wider MLP is explicitly paid for by narrower GQA. This iteration never reached training: the wrapper recorded `run_start` and then `run_abort` with `reason=missing_results_json`, no concrete run directory or `results.json` was created, and both `latest.json` and `best.json` stayed pinned to the earlier measured runs. Revert it for state hygiene only: from this clone, even forcing `CONFIG_JSON` to the last accepted run did not stop the wrapper from resolving repo-local `./data/...` shard/tokenizer paths, so this `kv2 + clean 3x MLP` hypothesis remains unmeasured until the repo-local data bridge or wrapper path plumbing is fixed.
- `3758396` / `ar5090-20260320-191102`: rerun that same `kv2 + clean 3x MLP` hypothesis on the accepted `stem=0, 1x2, tail=8, shared_mlp_hidden_bonus=192, warmdown_steps=80` line, this time with explicit sibling-repo shard and tokenizer overrides so the run was actually measured. The run completed successfully, but it still lost clearly at `val_bpb=1.651866244` versus the accepted `1.650638476` while increasing `artifact_bytes` to `13,892,208`, `num_params` to `20.72M`, and peak VRAM to `19.50 GB`, with essentially flat throughput (`83.5k` train / `324k` eval tok/s) and `313` steps. Revert it decisively: on this current retuned stemless deep-tail line, reducing GQA to `num_kv_heads=2` does not pay for a clean `mlp_mult=3` conversion, so deprioritize this exact byte-trade and look for materially different MLP or precision-policy reallocations instead.
- `db62aca` / `ar5090-20260320-192928`: test a compensated clean `mlp_mult=3` jump on the accepted stemless deep-tail line by shrinking `d_model: 512 -> 480` and `num_kv_heads: 4 -> 2` while removing the recurrent hidden bonus (`shared_mlp_hidden_bonus: 192 -> 0`). The run completed successfully and fit the 300-second budget cleanly with `318` steps plus slightly faster throughput (`85.2k` train / `329k` eval), but it still lost clearly at `val_bpb=1.659631479` versus the accepted `1.650638476`, while `artifact_bytes` jumped to `13,386,005`, `num_params` to `20.09M`, and peak VRAM to `19.14 GB`. Revert it decisively: on the current `stem=0, 1x2, tail=8, warmdown_steps=80` family, modest width/GQA compensation does not rescue a clean `3x` MLP conversion, so deprioritize further near-neighbor clean-`3x` probes unless they reclaim bytes much more aggressively or pair with a materially different precision/export policy.
- `22c4846` / `ar5090-20260320-194844`: test a base-width reallocation on the accepted stemless deep-tail line by widening `d_model: 512 -> 544` while trimming the MLP bonuses to keep absolute hidden sizes flat (`shared_mlp_hidden_bonus: 192 -> 128`, non-recurrent bonus `256 -> 192`). The run completed successfully, but it lost decisively at `val_bpb=1.674991999` versus the accepted `1.650638476`, with only `278` steps in the fixed 300-second budget, slower throughput (`71.4k` train / `296k` eval tok/s), higher peak VRAM (`19.67 GB`), and `artifact_bytes=12,583,606`, above the session's soft 12M ceiling. Revert it decisively: on the current `stem=0, 1x2, tail=8, shared_mlp_hidden_bonus=192, warmdown_steps=80` line, shifting bytes from bonus MLP width into a wider base model is far too expensive for the 5-minute 5090 tier, so deprioritize further `d_model`-up / bonus-down reallocations in this neighborhood.
- `2cf50ce` / `ar5090-20260320-200930`: keep the accepted `stem=0, 1x2, tail=8, shared_mlp_hidden_bonus=192, warmdown_steps=80` training stack fixed and make the export quantization config-visible by setting `low_bit_bits=6` on `mlp.fc.weight` and `mlp.proj.weight` only. The run completed successfully and only nudged `val_bpb` to `1.650194771` versus the prior accepted `1.650638476`, which is just a `0.000444` gain and below the normal accuracy-only promotion threshold, but it cut `artifact_bytes` from `11,860,072` to `8,891,640` and `compressed_model_bytes` from `11,733,263` to `8,763,017` with essentially unchanged steps (`316`), throughput (`84.0k` train / `322.8k` eval tok/s), params, and peak VRAM. Keep it as a byte-allocation win: simple MLP-only int6 export reclaims nearly `3 MB` of artifact headroom on the current best line, so future structural jumps should spend from this lower-byte baseline rather than reverting to full-int8 MLP export.
- `1a22bc1` / `ar5090-20260320-212546`: extend that accepted MLP-only int6 export on the current `stem=0, 1x2, tail=8, shared_mlp_hidden_bonus=192, warmdown_steps=80` baseline to include `attn.out_proj.weight` as well. The run completed successfully and preserved the training-budget fit (`315` steps, `83.8k` train / `322.7k` eval tok/s, `19.08 GB` peak VRAM), while cutting `artifact_bytes` further from `8,891,640` to `8,261,678` and `compressed_model_bytes` from `8,763,017` to `8,131,913`, but `val_bpb` regressed to `1.650638476` versus the accepted `1.650194771`. Revert it decisively: once the current line is already inside the artifact target band, this exact `attn.out_proj` int6 extension gives back too much validation quality for the extra byte savings, so future mixed-bit export probes should look for a gentler or differently placed low-bit expansion instead of repeating this exact pattern.
- `9f85dd6` / `ar5090-20260320-202556`: try spending part of that new int6 MLP-export headroom on training-time capacity by widening the stemless deep-tail line's unique non-recurrent MLP bonus from the implicit `d_model//2` (`256`) to `3*d_model//4` (`384`) while keeping the accepted `stem=0, 1x2, tail=8, shared_mlp_hidden_bonus=192, warmdown_steps=80` stack otherwise fixed. This iteration never reached training: the wrapper recorded `run_start` and then `run_abort` with `reason=missing_results_json`, no concrete run directory or `results.json` was created, and `latest.json` / `best.json` stayed pinned to `ar5090-20260320-200930`. Revert it for state hygiene only: this tail-width-spend hypothesis remains unmeasured from this clone until the repo-local `./data/...` shard bridge is restored or the wrapper is run with the working sibling-repo shard/tokenizer overrides again.
- `aa7f2c9` / `ar5090-20260320-203228`: rerun that same int6-headroom spend hypothesis with the working sibling-repo shard/tokenizer overrides so it was actually measured. The run completed successfully, but it lost clearly at `val_bpb=1.654179913` versus the accepted `1.650194771`, while increasing `artifact_bytes` to `9,190,288`, `num_params` to `20.51M`, peak VRAM to `19.31 GB`, and reducing the fixed-budget stop to `311` steps. Revert it decisively: on the current int6-MLP-export baseline, widening the non-recurrent tail MLP bonus from `256` to `384` spends bytes and compute without improving quality, so deprioritize this exact tail-width spend and look for a different way to use the reclaimed artifact headroom.
- `985096d` / `ar5090-20260320-214225`: spend the accepted int6-MLP-export headroom on attention capacity by raising the current stemless deep-tail line from `num_kv_heads: 4 -> 8` while keeping `stem=0, 1x2, tail=8`, `shared_mlp_hidden_bonus=192`, `warmdown_steps=80`, and MLP-only int6 export fixed. This iteration never reached training: the wrapper recorded `run_start` and then `run_abort` with `reason=missing_results_json`, no concrete run directory or `results.json` was created, and `latest.json` / `best.json` stayed pinned to the earlier measured runs. Revert it for state hygiene only: this full-KV-head probe remains unmeasured from this clone until the repo-local `./data/...` shard bridge is restored or the wrapper is launched with working shard/tokenizer paths again.
- `984f471` / `ar5090-20260321-000036`: on the accepted `stem=0, 1x2, tail=7, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=512, warmdown_steps=80` + MLP-only int6 export line, reallocate the repeated recurrent MLP width into one extra unique tail block by changing `shared_mlp_hidden_bonus: 192 -> 0` and `tail_layers: 7 -> 8`. This iteration never reached training: even with `CONFIG_JSON` pointed at the last accepted run's absolute-path config, `runpod_5090_train.sh` still aborted immediately with `No training shards matched TRAIN_PATTERN=/workspace/autoresearch-parameter-golf-ralph/data/datasets/fineweb10B_sp1024/fineweb_train_*.bin`, no concrete run directory or `results.json` was created, and `latest.json` / `best.json` stayed pinned to `ar5090-20260320-234437`. Revert it for state hygiene only: this depth-vs-recurrent-width reallocation remains unmeasured from this clone until the wrapper honors the accepted config's sibling-repo shard paths or a repo-local `./data` bridge is restored.
- `ca9f369` / `ar5090-20260321-000634`: rerun that same depth-vs-recurrent-width reallocation on the accepted `stem=0, 1x2, tail=7, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=512, warmdown_steps=80` + MLP-only int6 export line, this time with explicit sibling-repo shard/tokenizer overrides so it was actually measured. The run completed successfully and stayed inside the soft artifact band at `artifact_bytes=9,422,811`, but it lost clearly at `val_bpb=1.653141284` versus the accepted `1.637045167`, with fewer steps (`309` vs `340`), slower throughput (`81.8k` train / `318.4k` eval tok/s), higher peak VRAM (`19.43 GB`), and a larger model (`21.36M` params). Revert it decisively: on the current true-`3x` tail line, spending the repeated recurrent MLP bonus on one extra unique tail block is now measured and is a clear fixed-budget loser, so do not revisit this exact `shared_mlp_hidden_bonus: 192 -> 0`, `tail_layers: 7 -> 8` swap.
- `3741b5a` / `ar5090-20260321-040813`: on the accepted compact `seq_len=640`, `tail=4`, `6x` unique-tail MLP line (`stem=0, 1x2, tail=4, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=2048, warmdown_steps=80` + MLP-only int6 export), trade one unique tail block for materially wider remaining tail MLPs by changing `tail_layers: 4 -> 3` and `non_recurrent_mlp_hidden_bonus: 2048 -> 3072`, yielding a depth-5 `8x` unique-tail line. This won very clearly at `val_bpb=1.579516835` versus the accepted `1.591737349`, while also reducing `artifact_bytes` to `7,181,120`, `num_params` to `17.62M`, and peak VRAM to `13.17 GB`, and increasing the fixed-budget stop to `507` steps with faster throughput (`146.1k` train / `492.4k` eval tok/s). Keep it as the new accepted state: on the current compact int6 family, aggregate unique-tail MLP budget matters more than retaining the fourth unique tail block, so future probes should build on this new `tail=3`, `8x`, `seq_len=640` baseline rather than reverting to the older depth-6 layout.
- `8d745b2` / `ar5090-20260321-042227`: on the accepted compact `seq_len=640`, `tail=3`, `8x` unique-tail MLP line (`stem=0, 1x2, tail=3, shared_mlp_hidden_bonus=192, non_recurrent_mlp_hidden_bonus=3072, warmdown_steps=80` + MLP-only int6 export), continue the same depth-to-width path by trading one more unique tail block for `12x` unique tail MLPs (`tail_layers: 3 -> 2`, `non_recurrent_mlp_hidden_bonus: 3072 -> 5120`) while keeping aggregate unique-tail hidden budget flat. This iteration never reached training: the wrapper aborted immediately with `No training shards matched TRAIN_PATTERN=/workspace/autoresearch-parameter-golf-ralph/data/datasets/fineweb10B_sp1024/fineweb_train_*.bin`, no concrete run directory or `results.json` was created for `ar5090-20260321-042227`, and both `latest.json` and `best.json` stayed pinned to `ar5090-20260321-040813`. Revert it for state hygiene only: this compact `tail=2`, `12x` continuation remains unmeasured from this clone until the repo-local `./data` bridge is restored or the wrapper config already points at the working sibling-repo shard/tokenizer paths.
- `d2376e7` / `ar5090-20260321-043025`: rerun that same compact `tail=2`, `12x` continuation on the accepted `seq_len=640`, `1x2`, `tail=3`, `8x` unique-tail MLP line (`shared_mlp_hidden_bonus=192`, `non_recurrent_mlp_hidden_bonus=3072`, `warmdown_steps=80` + MLP-only int6 export), this time with explicit sibling-repo shard/tokenizer overrides so it actually measured. It won very clearly at `val_bpb=1.567765204` versus the accepted `1.579516835`, while reducing `artifact_bytes` to `6,725,753`, `num_params` to `16.83M`, and peak VRAM to `12.57 GB`, and increasing the fixed-budget stop to `586` steps with faster throughput (`176.5k` train / `576.0k` eval tok/s). Keep it as the new accepted state: on the compact int6 family, the depth-to-width trade still has headroom through this depth-4 `tail=2`, `12x` line, but the artifact now sits slightly below the session's soft 7M floor, so the next follow-up should likely spend some of the recovered byte or compute headroom rather than continuing to shrink the model.
- `7067588` / `ar5090-20260321-044541`: on the accepted compact `seq_len=640`, `1x2`, `tail=2`, `12x` unique-tail MLP line (`shared_mlp_hidden_bonus=192`, `non_recurrent_mlp_hidden_bonus=5120`, `warmdown_steps=80` + MLP-only int6 export), test the `mlp_mult` axis by raising the global MLP ratio from `2` to `3` while trimming the unique-tail bonus from `5120` to `4608` so the tail blocks stay at the measured `12x` width and the extra capacity is spent mainly in the repeated shared block. This iteration never reached training: `bash scripts/run_autoresearch_experiment.sh` launched with `CONFIG_JSON=/workspace/autoresearch-parameter-golf-ralph/configs/autoresearch_5090_5min.json`, then aborted immediately with `No training shards matched TRAIN_PATTERN=/workspace/autoresearch-parameter-golf-ralph/data/datasets/fineweb10B_sp1024/fineweb_train_*.bin`; no concrete run directory, `results.json`, or `crash.json` was created for this run, and both `latest.json` and `best.json` stayed pinned to `ar5090-20260321-043025`. Revert it for state hygiene only: this compact compensated global-`3x` MLP probe remains unmeasured from this clone until the repo-local `./data` bridge is restored or the wrapper again resolves to the working sibling-repo shard/tokenizer paths.
- `152e59e` / `ar5090-20260321-045223`: rerun that same compact compensated global-`3x` MLP probe on the accepted `seq_len=640`, `1x2`, `tail=2`, `12x` unique-tail MLP line (`shared_mlp_hidden_bonus=192`, `non_recurrent_mlp_hidden_bonus: 5120 -> 4608`, `warmdown_steps=80` + MLP-only int6 export), this time with explicit sibling-repo shard/tokenizer overrides so it actually measured. The run succeeded and became the numeric telemetry best at `val_bpb=1.567018049`, but that is only about `0.000747` better than the accepted `1.567765204`, below the session's `~0.001` meaningful-win threshold. It also increased `artifact_bytes` to `6,884,041`, raised `num_params` to `17.37M`, slowed throughput (`171.6k` train / `562.4k` eval tok/s versus `176.5k` / `576.0k`), and raised peak VRAM to `12.88 GB`, so revert it as below-threshold noise rather than promoting extra repeated MLP width on this compact line. `best.json` now points at this reverted run numerically, so branch tip plus `session.json` remain the authoritative accepted state.
- `b197965` / `ar5090-20260321-050633`: on the accepted compact `seq_len=640`, `1x2`, `tail=2`, `12x` unique-tail MLP line (`shared_mlp_hidden_bonus=192`, `non_recurrent_mlp_hidden_bonus=5120`, `warmdown_steps=80` + MLP-only int6 export), spend the recovered compute / VRAM headroom on longer context by raising `seq_len: 640 -> 768` while keeping `train_batch_tokens=val_batch_tokens=122880` fixed. This won clearly at `val_bpb=1.565102654` versus the accepted `1.567765204`, while also trimming `artifact_bytes` to `6,661,152`; throughput and fixed-budget fit fell (`165.5k` train / `538.1k` eval tok/s, `560` steps versus `176.5k` / `576.0k`, `586` steps) and peak VRAM rose modestly to `13.05 GB`, but the context gain still paid off cleanly. Keep it as the new accepted state: on the compact `tail=2`, `12x` line, the recovered budget is enough to support `seq_len=768`, so future structural probes should build from this longer-context baseline rather than reverting to `640`.
- `38bfd17` / `ar5090-20260321-052041`: on that accepted compact `seq_len=768`, `1x2`, `tail=2`, `12x` unique-tail MLP line, continue the same context-extension path by raising `seq_len: 768 -> 960` while keeping `train_batch_tokens=val_batch_tokens=122880` fixed and launching the wrapper with `CONFIG_JSON` pointed at the last accepted absolute-path config. This iteration still never produced telemetry from this workspace: `.autoresearch/experiments.jsonl` recorded `run_start` followed by `run_abort` with `reason=missing_results_json`, no concrete run directory or `results.json` was created for `ar5090-20260321-052041`, and both `latest.json` and `best.json` stayed pinned to `ar5090-20260321-050633`. Revert it for state hygiene only: this compact `seq_len=960` follow-up remains unmeasured here, so defer further longer-context continuation on this line until the wrapper once again yields structured results from the working shard/tokenizer setup.
- `52b9235` / `ar5090-20260321-052729`: rerun that same compact `seq_len=960` continuation on the accepted `seq_len=768`, `1x2`, `tail=2`, `12x` unique-tail MLP line, this time with explicit sibling-repo shard and tokenizer overrides so it actually measured. The run completed successfully, but it lost clearly at `val_bpb=1.570847874` versus the accepted `1.565102654`, while also cutting `artifact_bytes` further to `6,555,316`, reducing the fixed-budget stop to `514` steps, slowing throughput (`147.6k` train / `485.0k` eval tok/s versus `165.5k` / `538.1k`), and raising peak VRAM to `13.77 GB`. Revert it decisively: on the current compact `tail=2`, `12x` line, pushing context from `768` to `960` overspends the 5-minute 5090 budget and should be deprioritized unless another change first reclaims substantial compute.
- `e410b8d` / `ar5090-20260321-054159`: on the accepted compact `seq_len=768`, `1x2`, `tail=2`, `12x` unique-tail MLP line, de-share the lone recurrent pair into `2 shared x 1 loop` while keeping effective depth, batch tokens, and the MLP-only int6 export stack fixed. This iteration never reached training: the wrapper again resolved the repo-local `TRAIN_PATTERN=/workspace/autoresearch-parameter-golf-ralph/data/datasets/fineweb10B_sp1024/fineweb_train_*.bin`, then `.autoresearch/experiments.jsonl` recorded `run_abort` with `reason=missing_results_json`, no concrete run directory or `results.json` was created for `ar5090-20260321-054159`, and both `latest.json` and `best.json` stayed pinned to the earlier measured runs. Revert it for state hygiene only: this compact `2 shared x 1 loop` follow-up remains unmeasured from this clone until the wrapper again uses the working sibling-repo shard/tokenizer paths or a repo-local `./data` bridge is restored.
- `6e273c3` / `ar5090-20260321-054901`: rerun that same compact `seq_len=768`, `tail=2`, `12x` de-sharing hypothesis with explicit sibling-repo shard and tokenizer overrides so it actually measured. The run completed successfully and did move `artifact_bytes` up into the soft target band at `7,600,624`, but it still lost on the primary metric at `val_bpb=1.565732521` versus the accepted `1.565102654`, while adding about `0.94 MB`, increasing params to `18.87M`, dropping the fixed-budget stop to `556` steps, and slightly raising peak VRAM to `13.08 GB`. Revert it decisively: on the compact `seq_len=768`, `tail=2`, `12x` line, replacing the lone shared recurrent pair with two unique one-pass blocks is now measured and is not a good fixed-budget trade.
- `1c99cd0` / `ar5090-20260321-060421`: on the accepted compact `seq_len=768`, `1x2`, `tail=2`, `12x` unique-tail MLP line, continue the same depth-to-width path by trading the last unique tail block for a single much wider unique tail MLP while keeping aggregate unique-tail hidden budget flat (`tail_layers: 2 -> 1`, `non_recurrent_mlp_hidden_bonus: 5120 -> 11264`, i.e. `12x -> 24x`). The run completed successfully and reclaimed a large amount of fixed-budget compute (`663` steps, `206.0k` train / `651.7k` eval tok/s), but it lost badly at `val_bpb=1.574606030` versus the accepted `1.565102654`, while also shrinking `artifact_bytes` further to `6,076,843`, reducing params to `16.04M`, and raising peak VRAM to `15.69 GB`. Revert it decisively: on the compact `seq_len=768` line, the depth-to-width trend has now gone too far at `tail=1`, so future follow-ups should keep at least two unique tail blocks and spend budget through a different major axis.
- `eb59a6f` / `ar5090-20260321-061939`: on the accepted compact `seq_len=768`, `1x2`, `tail=2`, `12x` unique-tail MLP line, test the search policy's larger-MLP axis by raising the global MLP ratio from `2` to a true `3` while keeping `tail_layers=2`, `shared_mlp_hidden_bonus=192`, and `non_recurrent_mlp_hidden_bonus=5120` fixed. This iteration never reached training: even though the wrapper was launched with `CONFIG_JSON` pointed at the accepted run's absolute-path config, `runpod_5090_train.sh` still resolved `TRAIN_PATTERN=/workspace/autoresearch-parameter-golf-ralph/data/datasets/fineweb10B_sp1024/fineweb_train_*.bin`, then exited before creating a concrete run directory, `results.json`, or `crash.json`; `.autoresearch/experiments.jsonl` recorded `run_abort` with `reason=missing_results_json` and then `wrapper_cleanup`, while both `latest.json` and `best.json` stayed pinned to earlier runs. Revert it for state hygiene only: this compact `seq768` true-global-`3x` MLP jump remains unmeasured from this clone until the inner training script again honors the working sibling-repo shard/tokenizer paths or a repo-local `./data` bridge is restored.
- `e183d9a` / `ar5090-20260321-062633`: rerun that same compact `seq_len=768`, `1x2`, `tail=2`, `12x` unique-tail MLP hypothesis with explicit sibling-repo shard and tokenizer overrides so it actually measured. The run completed successfully and did move `artifact_bytes` into the session target band at `7,087,387`, but it lost clearly at `val_bpb=1.570406235` versus the accepted `1.565102654`, while also cutting the fixed-budget fit to `536` steps from `560`, slowing throughput (`156.7k` train / `517.8k` eval tok/s versus `165.5k` / `538.1k`), raising peak VRAM to `13.83 GB`, and increasing params to `18.42M`. Revert it decisively: on the compact `seq768`, `tail=2` line, a true global `mlp_mult=3` overspends the 5-minute 5090 budget, so future larger-MLP follow-ups should be more selective or more heavily compensated than this full-stack jump.
- `a7082a7` / `ar5090-20260321-064010`: on the accepted compact `seq_len=768`, `1x2`, `tail=2`, `12x` unique-tail MLP line, keep the two unique tail blocks fixed and spend the remaining budget only on the repeated shared block by raising `shared_mlp_hidden_bonus: 192 -> 512`, i.e. making the recurrent block a true `3x` MLP without widening the tail. The run completed successfully and was much cheaper than the full-stack `mlp_mult=3` jump, but it still lost at `val_bpb=1.565662031` versus the accepted `1.565102654`, with `artifact_bytes=6,756,973`, `550` steps, slower throughput (`161.9k` train / `530.5k` eval tok/s), slightly higher peak VRAM (`13.24 GB`), and `17.17M` params. Revert it decisively: on the compact `seq768`, `tail=2` line, shared-only recurrent widening is not enough on its own, so future larger-MLP follow-ups should pair any extra repeated-block width with a different major structural change rather than another hidden-bonus-only spend.
- `f63013a` / `ar5090-20260321-091500`: on the recovered compact `seq_len=768`, `1x1`, `tail=3`, `8x` unique-tail MLP line, test a compute-aware context curriculum by targeting `seq_len: 768 -> 832`, shrinking `train_batch_tokens=val_batch_tokens: 122880 -> 119808`, and keeping the first `160` train steps at `768` tokens before switching to full `832`. The run completed successfully and kept the step budget close to baseline (`566` steps), but it still lost clearly at `val_bpb=1.571341215` versus the accepted `1.570137665`, while slightly increasing `artifact_bytes` to `7,183,773`, slowing throughput (`163.6k` train / `541.8k` eval tok/s versus `168.6k` / `561.3k`), and trimming total tokens to `67.81M` from `69.30M`. Revert it decisively: on the recovered compact `tail=3`, `8x` carrier, this modest `768 -> 832` curriculum still overpays for longer context under the fixed 5090 budget, so the next structural follow-up should come from a different branch rather than another near-neighbor context nudge.
- `69922b3` / `ar5090-20260321-111727`: on the recovered compact `seq_len=768`, `1x1`, `tail=3`, `8x` unique-tail MLP line, test low-rank-Q reallocation by factorizing `q_proj` to rank `128` and spending the recovered matrix budget on a wider unique tail (`non_recurrent_mlp_hidden_bonus: 3072 -> 3584`, i.e. `8x -> 9x`). The run completed successfully and improved numerically to `val_bpb=1.569388948` versus the accepted `1.570137665`, but the gain was only about `0.000749`, below the session's `~0.001` meaningful-win threshold, while increasing `artifact_bytes` to `7,850,541`, params to `18.41M`, peak VRAM to `12.26 GB`, and slowing throughput (`162.3k` train / `549.2k` eval tok/s versus `168.6k` / `561.3k`). Revert it as below-threshold noise: this first low-rank-Q + wider-tail probe is viable, but future low-rank-Q follow-ups should pair the factorization with a more materially different carrier than a near-neighbor `8x -> 9x` tail-width spend.
- `370db14` / `ar5090-20260321-113258`: on the recovered compact `seq_len=768`, `1x1`, `tail=3`, `8x` unique-tail MLP line, test a more structural low-rank-Q reallocation by factorizing `q_proj` to rank `128`, adding one more unique tail block (`tail_layers: 3 -> 4`), and rebalancing unique-tail width from `8x` to `6x` (`non_recurrent_mlp_hidden_bonus: 3072 -> 2048`) to keep aggregate tail MLP budget flat. The run completed successfully but lost decisively at `val_bpb=1.582224335` versus the accepted `1.570137665`, while increasing `artifact_bytes` to `8,034,544`, cutting the fixed-budget fit to `480` steps / `58.98M` tokens, slowing throughput (`136.9k` train / `475.4k` eval tok/s), and raising peak VRAM to `12.82 GB`. Revert it decisively: on this compact carrier, using low-rank Q to buy a fifth block is much too compute-expensive, so future low-rank-Q follow-ups should spend the saved budget in a cheaper way than extra full-depth unique attention.
- `95305e4` / `ar5090-20260321-115223`: on the recovered compact `seq_len=768`, `1x1`, `tail=3`, `8x` line, test the search brief's "stronger carrier + low-rank Q" idea by factorizing `q_proj` to rank `128`, moving back onto a `1x2`, `tail=2` carrier, and spending the saved parameters on a wider `13x` unique tail (`shared_mlp_hidden_bonus: 0 -> 192`, `non_recurrent_mlp_hidden_bonus: 3072 -> 5632`). The run completed successfully but lost clearly at `val_bpb=1.576432352` versus the accepted `1.570137665`, while also shrinking `artifact_bytes` slightly to `7,095,425`, cutting the fixed-budget fit to `530` steps / `65.13M` tokens, slowing throughput (`156.0k` train / `528.9k` eval tok/s versus `168.6k` / `561.3k`), and raising peak VRAM to `13.49 GB`. Revert it decisively: on this branch, jumping straight to the stronger recurrent carrier while also adding low-rank Q and extra tail width over-mixes the move and still overspends compute, so the next stronger-carrier follow-up should isolate one of those reallocations instead of stacking all three at once.
- `376449c` / `ar5090-20260321-120803`: on the recovered compact `seq_len=768`, `1x1`, `tail=3`, `8x` line, test that isolated stronger-carrier follow-up by adding one more unique low-width carrier block (`shared_layers: 1 -> 2`) while keeping the `8x` unique tail, context, and export stack fixed. The run completed successfully but lost badly at `val_bpb=1.584993277` versus the accepted `1.570137665`, while increasing `artifact_bytes` to `7,870,131`, params to `19.25M`, peak VRAM to `13.68 GB`, and cutting the fixed-budget fit to `464` steps / `57.02M` tokens with slower throughput (`131.0k` train / `459.2k` eval tok/s). Revert it decisively: on this compact line, a plain extra `2x` carrier block overpays compute for too little representational gain, so future stronger-carrier probes should spend bytes more selectively than simple low-width depth insertion.
- `175c1f7` / `ar5090-20260321-122614`: on the recovered compact `seq_len=768`, `1x1`, `tail=3`, `8x` line, add a grouped causal local-token mixer to every unique tail block (`tail_local_kernel=5`) as a cheap structural probe of the local-module branch. The run completed successfully and stayed cheap (`artifact_bytes=7,497,463`, `17.86M` params, `11.87 GB` peak VRAM), but it still lost clearly at `val_bpb=1.574153379` versus the accepted `1.570137665`, while also fitting fewer fixed-budget steps / tokens (`535` / `65.74M` versus `564` / `69.30M`) and slower throughput (`158.4k` train / `541.0k` eval tok/s versus `168.6k` / `561.3k`). Revert it decisively: on this recovered compact carrier, this simple grouped tail local mixer adds enough compute drag to outweigh any local-bias benefit, so future local-module follow-ups should be cheaper or more selective than applying a kernel-5 mixer across the whole unique tail.
- `60c0412` / `ar5090-20260321-124251`: on the recovered compact `seq_len=768`, `1x1`, `tail=3`, `8x` line, factorize every `q_proj` to rank `128` and spend the recovered matrix budget on the lone carrier block by widening `shared_mlp_hidden_bonus: 0 -> 512` while leaving the `8x` unique tail unchanged. This won clearly at `val_bpb=1.567220420` versus the accepted `1.570137665`, beating the session threshold by about `0.00292` while keeping `artifact_bytes=7,613,658`, improving peak VRAM to `11.84 GB`, and preserving a healthy fixed-budget fit (`551` steps, `164.4k` train / `556.2k` eval tok/s). Keep it as the new accepted state: on this recovered compact carrier, low-rank Q pays when the saved budget is spent on a stronger carrier MLP rather than on more tail width, more tail depth, or a mixed stronger-carrier jump.
- `6c2fe04` / `ar5090-20260321-125834`: on the accepted compact low-rank-Q carrier, push the same reallocation harder by factorizing every `q_proj` from rank `128 -> 64` and widening the lone carrier MLP from true `3x` to `3.5x` (`shared_mlp_hidden_bonus: 512 -> 768`) while keeping the `8x` unique tail fixed. The run completed successfully but lost at `val_bpb=1.567759081` versus the accepted `1.567220420`, while also fitting slightly fewer fixed-budget steps / tokens (`546` / `67.09M` versus `551` / `67.71M`), slowing throughput a bit (`162.8k` train / `554.0k` eval tok/s versus `164.4k` / `556.2k`), and shrinking `artifact_bytes` to `7,193,475`. Revert it decisively: on this compact carrier, pushing low-rank Q past `128` to fund more shared-block MLP width over-compresses attention and gives back both quality and artifact budget, so keep `q_low_rank=128` as the stronger anchor if this branch is revisited.
- `9accb75` / `ar5090-20260321-131446`: on the accepted compact low-rank-Q carrier, keep `q_low_rank=128` and the true-`3x` shared block fixed, then spend more of the recovered budget on the unique tail by widening `non_recurrent_mlp_hidden_bonus: 3072 -> 3584` (`8x -> 9x`). The run completed successfully but lost badly at `val_bpb=1.573577367` versus the accepted `1.567220420`, while also increasing `artifact_bytes` to `8,001,572`, cutting the fixed-budget fit to `537` steps / `65.99M` tokens, slowing throughput (`159.2k` train / `542.9k` eval tok/s versus `164.4k` / `556.2k`), and raising peak VRAM to `12.42 GB`. Revert it decisively: on this current low-rank-Q carrier, a near-neighbor `8x -> 9x` unique-tail width spend is not the right next use of the recovered budget, so the next follow-up should move to a different major reallocation branch instead of another small tail-width nudge.
- `dace612` / `ar5090-20260321-133411`: on the accepted compact low-rank-Q carrier, test a more selective stronger-carrier rebalance by trading one `8x` unique tail block into a second unique true-`3x` carrier block (`shared_layers: 1 -> 2`, `tail_layers: 3 -> 2`) while widening the remaining tail pair to `10x` (`non_recurrent_mlp_hidden_bonus: 3072 -> 4096`) and keeping `q_low_rank=128`, context, and export settings fixed. The run completed successfully but still lost at `val_bpb=1.569126665` versus the accepted `1.567220420`, while also fitting fewer fixed-budget steps / tokens (`544` / `66.85M` versus `551` / `67.71M`), slowing throughput (`161.6k` train / `546.6k` eval tok/s versus `164.4k` / `556.2k`), and raising peak VRAM to `12.31 GB`; it only saved bytes to `artifact_bytes=7,504,066` by shrinking params to `16.90M`. Revert it decisively: on this current low-rank-Q compact line, even a compute-near-flat tail-to-carrier rebalance with wider remaining tails is still a losing stronger-carrier spend, so future follow-ups should avoid more carrier-vs-tail repartitions in this exact family and move to a different branch.
- `070ea36` / `ar5090-20260321-135023`: on the accepted compact low-rank-Q carrier, keep the architecture and export stack fixed but add a compute-aware training context curriculum (`train_seq_len_min=640` for the first `160` steps, then full `seq_len=768`). This won clearly at `val_bpb=1.562446671` versus the accepted `1.567220420`, while nudging `artifact_bytes` to `7,727,922`, improving the fixed-budget fit to `560` steps / `68.81M` tokens, slightly improving train throughput (`167.1k` tok/s), and leaving eval speed / peak VRAM essentially flat. Keep it: on this low-rank-Q compact line, a short-to-full context warmup is a clean production-plausible win and validates the compute-aware context-curriculum branch without adding structural complexity or overspending bytes.
- `8a9417a` / `ar5090-20260321-140537`: on the accepted compact low-rank-Q + context-warmup line, keep `q_low_rank=128`, `tail_layers=3`, `8x` unique-tail width, and the `640 -> 768` curriculum fixed, then widen the lone carrier MLP from true `3x` to `3.5x` (`shared_mlp_hidden_bonus: 512 -> 768`). The run completed successfully but lost clearly at `val_bpb=1.568727204` versus the accepted `1.562446671`, while also trimming the fixed-budget fit to `556` steps / `68.32M` tokens, slowing throughput (`165.5k` train / `552.6k` eval tok/s versus `167.1k` / `556.2k`), raising peak VRAM to `11.92 GB`, and nudging `artifact_bytes` up to `7,767,241`. Revert it decisively: on this warmed low-rank-Q carrier, the lone shared block does not have additional profitable headroom at `3.5x`, so future follow-ups should keep the current true-`3x` carrier width and spend bytes through a different axis.
- `2a9edea` / `ar5090-20260321-142120`: on the accepted compact low-rank-Q + context-warmup line, keep the carrier, tail, export stack, and `640 -> 768` curriculum fixed while relaxing `q_low_rank: 128 -> 160` to test whether the current factorization is slightly over-compressed. The run completed successfully but still lost clearly at `val_bpb=1.564883315` versus the accepted `1.562446671`, while also trimming the fixed-budget fit to `558` steps / `68.57M` tokens, slightly slowing throughput (`166.4k` train / `555.3k` eval tok/s versus `167.1k` / `556.2k`), nudging peak VRAM to `11.84 GB`, and shrinking `artifact_bytes` to `7,187,681`. Revert it decisively: on this warmed compact carrier, a softer low-rank-Q factorization does not recover enough attention quality to offset the small compute hit, so keep `q_low_rank=128` as the anchor if this branch is revisited.
- `09535e0` / `ar5090-20260321-143929`: on the accepted compact low-rank-Q + context-warmup line, keep the architecture, carrier width, tail width, and export stack fixed but disable train-time fake quant on the attention projections only (`attn_fake_quant_during_train=false`) while leaving MLP fake quant enabled. This won clearly at `val_bpb=1.558481296` versus the accepted `1.562446671`, improving by about `0.00397` while also fitting slightly more fixed-budget steps / tokens (`572` / `70.29M` versus `560` / `68.81M`), improving train throughput (`170.7k` tok/s), leaving eval speed / peak VRAM essentially flat, and only nudging `artifact_bytes` to `7,770,766`. Keep it as the new accepted state: on this warmed compact low-rank-Q carrier, selective attention-off fake quant is a cleaner training regime than the current blanket fake quant surrogate, so future quantization follow-ups should build on this selective-QAT baseline rather than restoring attention fake quant by default.
- `e5a8f2f` / `ar5090-20260321-145429`: on the accepted compact low-rank-Q + selective-attention-QAT-off line, keep the validated `640 -> 768` context warmup but raise the terminal context from `768 -> 1024`. The run completed successfully, but it lost badly at `val_bpb=1.573482967` versus the accepted `1.558481296`, while cutting the fixed-budget fit from `572` to `532` steps (`70.29M -> 65.37M` tokens), slowing throughput (`170.7k -> 152.9k` train tok/s, `555.8k -> 488.7k` eval tok/s), and raising peak VRAM from `11.83 GB` to `12.82 GB`; `artifact_bytes` stayed small at `7,730,645`. Revert it decisively: on this warmed compact low-rank-Q branch, `seq_len=1024` still overpays compute on the 5-minute 5090 tier, so future longer-context probes should wait for more compute reclamation or move to a different branch.
- `1048943` / `ar5090-20260321-151135`: on the accepted compact low-rank-Q + selective-attention-QAT-off line, keep the architecture and export stack fixed but delay the remaining MLP fake quant from step `20` to the full-context curriculum boundary at step `160`. The run completed successfully, but it still lost slightly at `val_bpb=1.559105445` versus the accepted `1.558481296`, while fitting one fewer fixed-budget step / token budget (`571` / `70.16M` versus `572` / `70.29M`); throughput and peak VRAM stayed essentially flat (`170.5k` train / `556.1k` eval tok/s, `11.83 GB`), and `artifact_bytes` only nudged up to `7,780,436`. Revert it decisively: on this warmed compact low-rank-Q branch, pushing MLP-only QAT all the way back to the full-context boundary leaves too little quantized adaptation to beat the current selective-QAT baseline, so any later-QAT follow-up should be less aggressive than step `160`.
- `b4dd1d7` / `ar5090-20260321-153116`: on the accepted compact low-rank-Q + selective-attention-QAT-off line, test a stronger-carrier depth reallocation by expanding the unique tail from `3 -> 5` blocks while reducing tail MLP width from `8x -> 4.5x` (`non_recurrent_mlp_hidden_bonus: 3072 -> 1280`) and keeping the true-`3x` shared block, `q_low_rank=128`, export stack, and `640 -> 768` curriculum fixed. The run completed successfully but lost badly at `val_bpb=1.585937277` versus the accepted `1.558481296`, while collapsing the fixed-budget fit to `438` steps / `53.82M` tokens, slowing throughput to `119.3k` train / `411.7k` eval tok/s, raising peak VRAM to `14.50 GB`, and increasing `artifact_bytes` to `8,647,735`. Revert it decisively: on this warmed low-rank-Q branch, adding two extra full-depth unique blocks is far too expensive for the 5-minute 5090 budget even after moderating tail width, so future stronger-carrier probes should reclaim much more compute first or use cheaper structural changes than extra full blocks.
- `92d13f3` / `ar5090-20260321-154606`: on the accepted compact low-rank-Q + selective-attention-QAT-off line, keep the architecture, low-rank-Q factorization, and export stack fixed but make the compute-aware curriculum more aggressive by lowering the warmup context from `640 -> 512` for the first `160` train steps before returning to full `768`. The run completed successfully and bought a modest fixed-budget fit increase (`582` steps / `71.52M` tokens versus `572` / `70.29M`) with slightly faster train throughput (`173.5k` tok/s) and essentially unchanged eval speed / peak VRAM, but it still lost clearly at `val_bpb=1.567244722` versus the accepted `1.558481296`, while nudging `artifact_bytes` up to `7,911,607`. Revert it decisively: on this selective-QAT low-rank-Q carrier, pushing the warmup context all the way down to `512` over-optimizes for extra cheap early tokens and gives back too much full-context quality, so keep `640 -> 768` as the current curriculum anchor and move the next follow-up to a different branch.
- `3757441` / `ar5090-20260321-160356`: on the accepted compact low-rank-Q + selective-attention-QAT-off line, test a cheaper local-module branch by adding a grouped causal local mixer only to the final unique tail block (`tail_local_kernel=3`) while keeping the `640 -> 768` curriculum, `q_low_rank=128`, and MLP widths fixed. The run completed successfully but still lost at `val_bpb=1.558943259` versus the accepted `1.558481296`, while cutting the fixed-budget fit from `572` to `559` steps (`70.29M -> 68.69M` tokens), slowing throughput (`170.7k -> 166.3k` train tok/s, `555.8k -> 549.1k` eval tok/s), and nudging peak VRAM up slightly to `11.90 GB`; `artifact_bytes` stayed near-flat at `7,736,757`. Revert it decisively: on this warmed selective-QAT low-rank-Q carrier, even a last-tail-only kernel-3 local mixer still costs more compute than quality it returns, so future local-module work should be cheaper than a persistent conv branch or moved to a different carrier family.
- `e8c4cd1` / `ar5090-20260321-162003`: on the accepted compact low-rank-Q + selective-attention-QAT-off line, disable MLP fake quant only on the lone shared carrier block while leaving the three large unique tail MLPs quantization-aware. The run completed successfully and did improve telemetry to `val_bpb=1.557883819` versus the accepted `1.558481296`, with slightly better throughput (`170.9k` train / `556.1k` eval tok/s), one extra step (`573`), and slightly smaller `artifact_bytes=7,760,474`, but the gain was only about `0.000597`, below the session's `0.001` meaningful-win threshold. Revert it as noise: this shared-block-only QAT relaxation is directionally interesting on the current compact carrier, but it adds code complexity without a large enough measured payoff, so any follow-up should pair it with a more structural or byte-allocation change rather than accept it standalone.
- `415777b` / `ar5090-20260321-163653`: on the accepted compact low-rank-Q + selective-attention-QAT-off line, disable train-time MLP fake quant only on the final unique tail block while keeping the `640 -> 768` curriculum, `q_low_rank=128`, and the other three blocks quantization-aware. The run completed successfully and improved numerically to `val_bpb=1.557919481` versus the accepted `1.558481296`, with essentially identical fixed-budget fit (`573` steps / `70.41M` tokens), near-flat throughput and peak VRAM (`170.9k` train / `555.7k` eval tok/s, `11.83 GB`), and slightly larger `artifact_bytes=7,766,600`, but the gain was only about `0.000562`, below the session's `0.001` meaningful-win threshold. Revert it as noise: selective final-tail MLP QAT relaxation is directionally positive on this compact carrier, but it is still too small to accept standalone and should only be revisited if paired with a more structural or byte-allocation change.
- `af96ca2` / `ar5090-20260321-165102`: on the accepted compact low-rank-Q + selective-attention-QAT-off line, combine the two directionally positive coarse selective-QAT probes by disabling train-time MLP fake quant on both the lone shared carrier block and the final unique tail block while keeping the `640 -> 768` curriculum, `q_low_rank=128`, and the rest of the export stack fixed. The run completed successfully and improved numerically to `val_bpb=1.557900740` versus the accepted `1.558481296`, while fitting two extra fixed-budget steps / tokens (`574` / `70.53M` versus `572` / `70.29M`), slightly improving train throughput (`171.3k` tok/s), leaving eval speed / peak VRAM flat (`556.1k` eval tok/s, `11.83 GB`), and nudging `artifact_bytes` up to `7,784,712`; however, the gain was only about `0.000581`, still below the session's `0.001` meaningful-win threshold and slightly worse than the prior best reverted telemetry from the shared-block-only probe (`1.557883819`). Revert it as noise: stacking these two blockwise MLP-QAT relaxations is still too small to accept on this compact carrier, so the next quantization follow-up should be coarser or paired with a more structural carrier change rather than adding more tiny block-level carveouts.
- `1f3b461` / `ar5090-20260321-170512`: on the accepted compact low-rank-Q + selective-attention-QAT-off line, trade the current `tail=3`, `8x` unique-tail MLP stack into `tail=4`, `6x` while keeping the true-`3x` shared block, `q_low_rank=128`, and the `640 -> 768` curriculum fixed. The run completed successfully but lost badly at `val_bpb=1.579765397` versus the accepted `1.558481296`, while increasing `artifact_bytes` to `8,336,514`, cutting the fixed-budget fit to `494` steps / `60.70M` tokens, slowing throughput to `140.5k` train / `470.6k` eval tok/s, and raising peak VRAM to `12.98 GB`. Revert it decisively: on this warmed low-rank-Q compact carrier, adding a fourth full-depth unique tail block even with `6x` tail MLPs is still far too expensive for the 5-minute 5090 budget, so future stronger-carrier probes should reclaim much more compute first or use cheaper structural spends than another full extra block.
- `a8d7cd4` / `ar5090-20260321-172400`: on the accepted compact low-rank-Q + selective-attention-QAT-off line, keep the carrier, tail, export stack, and `640 -> 768` curriculum fixed but shrink the global train batch from `122880` at `grad_accum_steps=4` to `92160` at `grad_accum_steps=3`, holding the per-micro batch at `30720` so both 1-GPU and 8-GPU divisibility still work. The run completed successfully and won very clearly at `val_bpb=1.547635883` versus the accepted `1.558481296`, while fitting far more optimizer updates (`748` steps versus `572`) despite slightly lower token throughput and total tokens (`167.4k` train tok/s, `68.94M` tokens versus `170.7k`, `70.29M`), leaving eval speed / peak VRAM essentially flat (`556.1k` eval tok/s, `11.83 GB`), and nudging `artifact_bytes` up to `8,188,771`. Keep it as the new accepted state: on this warmed selective-QAT low-rank-Q carrier, smaller global updates with the same microbatch are a strong fixed-budget win, so future compute-aware follow-ups should treat this `3 x 30720` update shape as the new anchor rather than restoring the larger `4 x 30720` global batch.
- `e3499d7` / `ar5090-20260321-173834`: on the accepted compact low-rank-Q + selective-attention-QAT-off + smaller-update-batch line, make named float-passthrough tensors bypass the small-tensor cutoff and keep `tok_emb.weight` in float16 export so the tied embedding / head is actually tested at higher precision. The run completed successfully and became the numeric telemetry best at `val_bpb=1.547509981` versus the accepted `1.547635883`, but the gain was only about `0.000126`, far below the session's `0.001` meaningful-win threshold, while `artifact_bytes` rose to `8,793,299` with essentially unchanged throughput / VRAM (`167.4k` train / `556.2k` eval tok/s, `11.83 GB`). Revert it as noise: selective float tied-embedding export is directionally positive on this carrier, but too small to accept standalone, so any revisit should pair it with a larger structural or byte-allocation change rather than promoting it by itself. `best.json` now points at this reverted run numerically, so branch tip plus `session.json` remain the authoritative accepted state.
- `c97a031` / `ar5090-20260321-175556`: on the accepted compact low-rank-Q + selective-attention-QAT-off + smaller-update-batch line, push the same compute-aware update-batch direction one step further by shrinking the global train batch from `92160` at `grad_accum_steps=3` to `61440` at `grad_accum_steps=2`, again holding the per-micro batch at `30720` so 1-GPU and 8-GPU divisibility remain intact. The run completed successfully and won very clearly at `val_bpb=1.536442380` versus the accepted `1.547635883`, while fitting far more optimizer updates (`1078` steps versus `748`) despite lower train throughput and total tokens (`160.9k` train tok/s, `66.23M` tokens versus `167.4k`, `68.94M`), leaving eval speed / peak VRAM flat (`556.1k` eval tok/s, `11.83 GB`) and keeping `artifact_bytes=8,650,546`. Keep it as the new accepted state: on this warmed selective-QAT low-rank-Q carrier, smaller global updates with the same microbatch still have not saturated, so future follow-ups should treat `2 x 30720` as the new compute-aware batch anchor rather than restoring the larger update shapes.
- `65784f7` / `ar5090-20260321-181315`: on the accepted compact low-rank-Q + selective-attention-QAT-off + `2 x 30720` update-batch line, delay MLP fake quant from step `20` to the full-context boundary at step `160` while keeping the `640 -> 768` curriculum, `q_low_rank=128`, and the export stack fixed. The run completed successfully and won clearly at `val_bpb=1.535333866` versus the accepted `1.536442380`, beating the session threshold by about `0.00111`, while fitting `1086` steps / `66.72M` tokens, holding throughput and peak VRAM essentially flat (`162.1k` train / `556.2k` eval tok/s, `11.83 GB`), and keeping `artifact_bytes=8,669,498`. Keep it as the new accepted state: on this small-batch selective-QAT carrier, delaying MLP fake quant to the full-context boundary is now a validated late-quantization win, so future quantization follow-ups should build from `fake_quant_start_step=160` rather than restoring earlier MLP-QAT timing.
- `1254d06` / `ar5090-20260321-182933`: on the accepted compact low-rank-Q + selective-attention-QAT-off + `2 x 30720` update-batch + late-MLP-QAT line, spend the recovered update headroom on a longer-context curriculum by lifting training from `640 -> 768` to `768 -> 1024` and shrinking the global train batch from `61440` to `49152` so 1-GPU and 8-GPU divisibility still hold. The run completed successfully but lost decisively at `val_bpb=1.543832969` versus the accepted `1.535333866`, while `artifact_bytes` rose to `8,843,883`, train/eval throughput fell to `140.6k` / `488.7k` tok/s, total tokens dropped to `60.01M`, and peak VRAM eased slightly to `11.51 GB`. Revert it decisively: on this current small-batch low-rank-Q carrier, a direct `1024` context jump is still too compute-expensive for the 5-minute 5090 budget, so longer-context follow-ups should reclaim more compute first or use a milder context lift than `1024`.
- `a2342e4` / `ar5090-20260321-184323`: on the accepted compact low-rank-Q + selective-attention-QAT-off + `2 x 30720` update-batch + late-MLP-QAT line, keep the carrier, `640 -> 768` curriculum, and export stack fixed but shrink the global train batch one step further from `61440` at `grad_accum_steps=2` to `30720` at `grad_accum_steps=1`, holding the per-micro batch at `30720` so 1-GPU and 8-GPU divisibility still work. The run completed successfully and fit far more optimizer updates (`1984` steps versus `1086`), but it still lost clearly at `val_bpb=1.539224717` versus the accepted `1.535333866`, while `artifact_bytes` rose to `9,533,258`, train throughput fell to `148.1k` tok/s, total tokens dropped to `60.95M`, and peak VRAM stayed flat at `11.77 GB`. Revert it decisively: on this late-MLP-QAT small-batch carrier, going all the way down to a single `30720`-token microbatch overshoots into too-noisy updates, so future compute-aware follow-ups should keep at least `2 x 30720` and move the next probe to a different structural or coarse quantization branch.
- `d9f225d` / `ar5090-20260321-185813`: on the accepted compact low-rank-Q + selective-attention-QAT-off + `2 x 30720` update-batch + late-MLP-QAT line, disable train-time MLP fake quant on all non-recurrent blocks while keeping the shared carrier block on the current step-`160` late-QAT schedule. The run completed successfully but lost slightly at `val_bpb=1.535536343` versus the accepted `1.535333866`, while `artifact_bytes` stayed effectively flat at `8,669,678`, fixed-budget fit only nudged up to `1090` steps / `66.97M` tokens, throughput and eval speed stayed essentially flat (`162.7k` train / `556.1k` eval tok/s), and peak VRAM was unchanged at `11.84 GB`. Revert it: coarse non-recurrent MLP-QAT removal is not a win on this late-QAT small-batch carrier, so future selective-QAT follow-ups should be more targeted than disabling all unique-block MLP fake quant at once.
- `043c293` / `ar5090-20260321-191208`: on the accepted compact low-rank-Q + selective-attention-QAT-off + `2 x 30720` update-batch + late-MLP-QAT line, push `q_low_rank: 128 -> 96` and spend the recovered budget on a milder shared-carrier MLP widen (`shared_mlp_hidden_bonus: 512 -> 640`). The run completed successfully but lost clearly at `val_bpb=1.539332349` versus the accepted `1.535333866`, while also fitting slightly fewer fixed-budget steps / tokens (`1081` / `66.42M` versus `1086` / `66.72M`), slowing throughput a bit (`161.2k` train / `554.7k` eval tok/s versus `162.1k` / `556.2k`), nudging peak VRAM up to `11.87 GB`, and shrinking `artifact_bytes` to `8,462,007`. Revert it decisively: on this current late-QAT small-batch carrier, compressing Q further to fund a `3.25x` shared block under-spends bytes and gives back quality, so future low-rank-Q follow-ups should avoid harder Q compression unless the spend is materially stronger or lands on a different carrier branch.
- `17845d0` / `ar5090-20260321-192709`: on the accepted compact low-rank-Q + selective-attention-QAT-off + `2 x 30720` update-batch + late-MLP-QAT line, add one more full-width unique tail block (`tail_layers: 3 -> 4`) while keeping the current widths, `640 -> 768` curriculum, and export stack fixed. The run completed successfully but lost decisively at `val_bpb=1.548908352` versus the accepted `1.535333866`, while spending bytes up to `artifact_bytes=10,555,357`, dropping fixed-budget fit to `874` steps / `53.70M` tokens, slowing throughput to `122.3k` train / `446.3k` eval tok/s, and raising peak VRAM to `14.41 GB`. Revert it decisively: on this late-MLP-QAT small-batch carrier, adding a raw extra `8x` unique tail block is too compute-expensive for the 5-minute 5090 budget, so future stronger-carrier probes should reclaim compute at the same time rather than stacking depth alone.
- `c84fcc6` / `ar5090-20260321-194330`: on the accepted compact low-rank-Q + selective-attention-QAT-off + `2 x 30720` update-batch + late-MLP-QAT line, test the note's suggested milder context follow-up by lifting full context from `768` to `960` while keeping the `640 -> full` curriculum, `q_low_rank=128`, and export stack fixed. The run completed successfully but lost decisively at `val_bpb=1.566337306` versus the accepted `1.535333866`, while shrinking `artifact_bytes` slightly to `8,651,863`, cutting the fixed-budget fit to `1013` steps / `62.24M` tokens, slowing throughput to `146.7k` train / `499.6k` eval tok/s, and raising peak VRAM to `12.56 GB`. Revert it decisively: on this late-MLP-QAT small-batch carrier, even the milder `960` context lift still overspends the 5-minute 5090 budget, so longer-context retries need materially more compute reclamation or a different carrier branch rather than another near-neighbor context increase.
- `9fbe60b` / `ar5090-20260321-200209`: on the accepted compact low-rank-Q + selective-attention-QAT-off + `2 x 30720` update-batch + late-MLP-QAT line, split MLP fake-quant timing so the shared carrier block stays at step `160` while all non-recurrent blocks delay to step `240`. The run completed successfully and improved numerically to `val_bpb=1.535142815` versus the accepted `1.535333866`, while keeping `artifact_bytes=8,671,602`, fixed-budget fit (`1087` steps / `66.79M` tokens), throughput (`162.2k` train / `556.1k` eval tok/s), and peak VRAM (`11.84 GB`) essentially flat; however, the gain was only about `0.000191`, far below the session's `0.001` meaningful-win threshold. Revert it as noise: selective later non-recurrent MLP QAT is directionally positive on this carrier, but too small to accept standalone, so any revisit should pair it with a larger structural or coarser selective-quantization change rather than promoting this timing split by itself.
- `6101f83` / `ar5090-20260321-201809`: on the accepted compact low-rank-Q + selective-attention-QAT-off + `2 x 30720` update-batch + late-MLP-QAT line, add a single adapter slot to every non-recurrent block so the three unique tail blocks also carry rank-8 `{q,k,v,attn_out,mlp_in,mlp_out}` adapters instead of reserving adapters for the lone shared carrier block only. The run completed successfully but lost catastrophically at `val_bpb=1.602012026` versus the accepted `1.535333866`, while slowing throughput to `135.8k` train / `489.7k` eval tok/s, cutting the fixed-budget fit to `943` steps / `57.94M` tokens, and raising peak VRAM to `12.60 GB`; `artifact_bytes` stayed low at `8,563,391` despite the extra `0.30M` params. Revert it decisively: on this small-batch late-QAT low-rank-Q carrier, broad tail adapter activation is far too expensive in fixed-budget compute for the quality it returns, so future adapter follow-ups should be much narrower than turning on full-target adapters across every unique block.
- `e9f76ac` / `ar5090-20260321-203617`: on the accepted compact low-rank-Q + selective-attention-QAT-off + `2 x 30720` update-batch + late-MLP-QAT line, keep the shared block's rank-8 full-target loop adapters intact but add narrow single-slot non-recurrent output adapters on the unique tail blocks (`rank=4`, `{attn_out, mlp_out}`). The run completed successfully but still lost decisively at `val_bpb=1.551966782` versus the accepted `1.535333866`, while `artifact_bytes=8,617,075` stayed low, fixed-budget fit fell to `995` steps / `61.13M` tokens, throughput slowed to `146.2k` train / `526.0k` eval tok/s, and peak VRAM rose to `12.32 GB`. Revert it decisively: even much narrower tail-only output adapters are still a bad trade on this late-QAT small-batch carrier, so future adapter work should move off tail-adapter activation and onto a different structural or selective-quantization branch.
- `c8ca51e` / `ar5090-20260321-205316`: on the accepted compact low-rank-Q + selective-attention-QAT-off + `2 x 30720` update-batch + late-MLP-QAT line, restore full-rank `q_proj` only in the lone shared carrier block while keeping the three unique tail blocks at `q_low_rank=128`, with the `640 -> 768` curriculum and export stack fixed. The run completed successfully but lost clearly at `val_bpb=1.540049116` versus the accepted `1.535333866`, while fixed-budget fit, throughput, and peak VRAM stayed essentially flat (`1085` steps / `66.66M` tokens, `161.9k` train / `555.3k` eval tok/s, `11.86 GB`) and `artifact_bytes` only dipped to `8,613,164`. Revert it decisively: on this late-MLP-QAT small-batch carrier, the shared block does not recover enough quality from full-rank Q to justify even the modest extra attention spend, so future low-rank-Q follow-ups should look for a different selective allocation than simply restoring the carrier block to full rank.
- `6496b05` / `ar5090-20260321-210915`: on the accepted compact low-rank-Q + selective-attention-QAT-off + `2 x 30720` update-batch + late-MLP-QAT line, restore full-rank `q_proj` only in the final unique tail block while keeping the shared carrier block and the earlier tail blocks at `q_low_rank=128`. The run reached the fixed-budget stop at step `1088`, but the wrapper finished as `status=failed` with `error_type=KeyboardInterrupt` during final validation (`eval_val`) before a usable summary or artifact was written, so `results.json` recorded no `val_bpb` / artifact bytes and the experiment was reverted for state hygiene only. Treat this hypothesis as unmeasured from branch memory until it is rerun cleanly without the interrupted post-train validation path.
- `e10e737` / `ar5090-20260321-212309`: on the accepted compact low-rank-Q + selective-attention-QAT-off + `2 x 30720` update-batch + late-MLP-QAT line, reallocate Q rank from the lone shared carrier block into the final unique tail block by setting `shared_q_low_rank=96` while restoring the final tail block to full-rank `q_proj`. The run completed cleanly and nudged telemetry to `val_bpb=1.535289967` versus the accepted `1.535333866`, with identical fixed-budget fit (`1087` steps / `66.79M` tokens), near-flat throughput (`162.3k` train / `556.3k` eval tok/s), slightly lower peak VRAM (`11.83 GB`), and a smaller artifact (`8,537,588` bytes). Revert it as below-threshold noise: the gain was only about `0.000044`, which is far below the session's `0.001` meaningful-win bar, and it adds selective-Q-rank complexity while the current line already under-spends the artifact target band.
- `90c723c` / `ar5090-20260321-214012`: on the accepted compact low-rank-Q + selective-attention-QAT-off + `2 x 30720` update-batch + late-MLP-QAT line, trade one unit of unique-tail MLP width for an extra unique block by shifting `tail_layers: 3 -> 4` while shrinking `non_recurrent_mlp_hidden_bonus: 3072 -> 2048`, keeping the shared carrier at `3x`, `q_low_rank=128`, and the `640 -> 768` late-MLP-QAT curriculum fixed. The run completed successfully but lost decisively at `val_bpb=1.550574032` versus the accepted `1.535333866`, while `artifact_bytes` rose to `9,194,613`, fixed-budget fit fell to `935` steps / `57.45M` tokens, throughput dropped to `133.0k` train / `470.8k` eval tok/s, and peak VRAM rose to `12.98 GB`. Revert it decisively: on this current small-batch low-rank-Q carrier, extra unique depth still costs too much even after trimming tail MLP width from `8x` to `6x`, so future stronger-carrier probes need materially more compute reclamation or a different carrier family rather than another local depth-for-width swap.
